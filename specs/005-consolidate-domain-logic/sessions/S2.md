# Session S2 — feature 005, the mechanical half (T009–T021)

> **How to use this file**: start a fresh Claude session in the worktree and say
> "execute `specs/005-consolidate-domain-logic/sessions/S2.md`". No driver
> script. Nothing parses your output.

You are executing session S2 of feature `005-consolidate-domain-logic`. You have
no memory of earlier sessions. S1 did T001–T008 and left you a record.

## Where you are

The git worktree at `.claude/worktrees/004-p3-workbench-shell`, on branch
`004-p3-workbench-shell`. Feature 005 has no branch of its own.

Confirm `.specify/feature.json` points at
`specs/005-consolidate-domain-logic`. It is gitignored, so it does not travel.

Ignore `setup-tasks.sh` and `check-prerequisites.sh` — they resolve
`FEATURE_DIR` from the branch name against the main checkout and report the
wrong feature in the wrong tree.

## Orient first

1. `specs/005-consolidate-domain-logic/sessions/handoff.md` — **read this
   first.** S1 wrote what it decided and what it found hard.
2. `specs/005-consolidate-domain-logic/triage-record.md` — **this is your
   instruction set.** Every decision is already made. Your job is to carry them
   out, not to revisit them.
3. `git log --oneline main..HEAD` — what actually landed, as opposed to what was
   planned
4. `.specify/memory/constitution.md`
5. `specs/005-consolidate-domain-logic/research.md` — **D1 governs where every
   survivor goes and what it mounts**
6. `specs/005-consolidate-domain-logic/tasks.md`

## Your scope: T009 through T021

- **T009–T012** — Move the survivors into behavior-named files. Parallel: four
  distinct new files, no shared state.
- **T013** — Delete both departing test files and the four `layoutMode` cases.
- **T014** — Verify FR-008 mechanically.
- **T015** — `test-quality-reviewer` on the new files.
- **T016** — Gate, and reconcile the case count.
- **T017** — B1–B8, confirm zero drift.
- **T018** — Update 004's session prompts.
- **T019** — Point `.specify/feature.json` back at 004.
- **T020** — Write the `## S2` handoff section.
- **T021** — Feature checkpoint **(the user commits this one)**.

## The rule that governs T009–T012

From research D1: **a survivor mounts the smallest surviving component that
exhibits its behavior, never a page.**

`src/components/sections/` holds twelve components and 004's T020 deletes
exactly two — `TemplateSelector.tsx` and `ActionButtons.tsx`. The other ten
survive and are re-homed into the workbench. So there is almost always a
component that is not going anywhere:

| Cluster | Mounts | New file |
|---|---|---|
| save / load / share, dropped-placement notice, oversize warning | `SaveLoadShare` | `saveLoadShare.test.tsx` |
| Validation and Warnings headings, flighting Accept / Reject | `AnalysisOutput` | `analysisOutput.test.tsx` |
| schedule rows, empty state, refs deriving from placements, `days_available` flagging | `ScheduleOutput` | `scheduleOutput.test.tsx` |
| strips, fencer counts, overrides, tournament setup | the four rail sections, individually | `configEditing.test.tsx` |

**The four buckets are provisional.** The triage record decides how many there
really are. A bucket holding one or two cases merges into its neighbour; a
bucket the triage did not populate is not created. Check S1's handoff — it says
whether these survived contact.

A genuinely cross-section flow composes its own minimal host inside the test
file. It does not import `KitchenSinkPage`.

**Preserve accessible-output assertions.** The `role="alert"` load error and the
`role="status"` dropped-placement notice are coverage a store-level test cannot
provide. Do not demote them.

## What you must not delete

T013 deletes **test files only**:

- `__tests__/components/KitchenSinkPage.test.tsx`
- `__tests__/components/WizardShell.test.tsx`
- the four `layoutMode` cases in `__tests__/store/store.test.ts`

**Leave every source file alone**, including `src/store/store.ts`'s `layoutMode`
slice. `src/App.tsx:60` is still
`{layoutMode === 'wizard' ? <WizardShell /> : <KitchenSinkPage />}` with no third
branch — those components are the entire application until 004's T011–T019 build
the workbench. Deleting them here leaves a header rendering over nothing.
Feature 004's T020 owns their removal (research D6).

Run T013 only after T009–T012, so nothing is deleted before its survivors have a
home.

## Halt conditions

- **T016** — the final case count will not reconcile to `878 − deleted + added`.
  A green suite at an unexplained count is the exact failure this feature exists
  to prevent. Report the discrepancy; never adjust a count to close it.
- **T017** — any B1–B8 movement at all, including an increase. No engine file
  was touched, so drift means a defect in this work.
- **T014** — a `grep` hit you cannot remove without deleting real coverage.
- The triage record turns out to be wrong about a case. Say so; do not silently
  re-decide it. S1 made that call with context you do not have.

## Rules that bind you

- **Constitution I** — `src/engine/` is not read or written.
- **Constitution II** — dispatch `test-quality-reviewer` at T015.
  `react-code-reviewer` has nothing to review; no component is written.
- **Constitution III** — zero drift. T017 is proof, not a diff to explain.
- **Constitution VI** — `scripts/smoke.mjs` is **neither run nor modified**.
  Nothing user-visible changes here, and its locators all target UI that 004's
  T020 deletes.
- **T018 may edit only 004's `sessions/S2.md` and `S3.md`.** Not its `tasks.md`,
  `plan.md`, `spec.md`, or `research.md` — the re-plan hook halts you, and it is
  right to.
- **Git** — worktree, so you and your subagents commit to
  `004-p3-workbench-shell`. Never `push`, `merge`, `rebase`, `reset --hard`, or
  delete a branch. **T021 is the user's commit, not yours.**
- **Orchestration** — no code beyond a 1–5 line edit; dispatch to subagents with
  an explicit `model: sonnet`.

## On T018 specifically

Rewrite 004's `S3.md` so it no longer instructs 78 judgment calls and no longer
cites the superseded 52 and 79 counts. Point it at `triage-record.md` instead,
and reduce its T021–T022 brief to re-pointing already-triaged tests at the
workbench.

Note that `scripts/run-chain.sh` is no longer in use — the session-chaining
experiment was abandoned. Do not spend effort updating its header comment. If
the script is still in the tree, leave it; removing it is the user's call.

## GitHub issues

Your scope maps to **#173–#185**, labeled `005-consolidate-domain-logic`. Close
each as its commits land. Task IDs repeat per feature — scope by label, always.

## Before you finish

1. Run the gate, suite **twice**:
   ```
   timeout 180 pnpm exec tsc -b   > ./tmp/tsc.log  2>&1
   timeout 120 pnpm --silent lint > ./tmp/lint.log 2>&1
   timeout 120 pnpm --silent test > ./tmp/test.log 2>&1
   ```

2. Confirm both return nothing:
   ```
   grep -rn "KitchenSinkPage\|WizardShell\|layoutMode" __tests__/
   find __tests__ -iname "*kitchensink*" -o -iname "*wizard*"
   ```

3. Append a `## S2` section to `sessions/handoff.md` covering:
   - tasks completed, with commit SHAs
   - the final re-targeted / deleted tally and where survivors went
   - **any coverage knowingly dropped, and why** — the thing most worth writing
     down and the easiest to leave out
   - the final suite count and how it reconciles against 878
   - what changed in 004's session prompts
   - anything not finished, and why

4. Commit the handoff. **Stop before T021** — hand the branch back with a
   paste-ready prompt for 004's S2.

## Report back

Plain summary for the human: the tally, the reconciliation arithmetic, any
coverage dropped, and confirmation that 004's T020 is now a source-only
deletion. No JSON.
