# Session S1 — feature 005, the judgment half (T001–T008)

> **How to use this file**: start a fresh Claude session in the worktree and say
> "execute `specs/005-consolidate-domain-logic/sessions/S1.md`". No driver
> script. Nothing parses your output — write your findings for the next human
> and the next session, not for a machine.

You are executing session S1 of feature `005-consolidate-domain-logic`. You have
no memory of earlier sessions. Everything you need is on disk.

## Where you are

The git worktree at `.claude/worktrees/004-p3-workbench-shell`, on branch
`004-p3-workbench-shell`. Feature 005 is interstitial and has **no branch of its
own** — it lands on 004's branch, before that feature's S2 begins. Do not create
a worktree or a branch.

Confirm `.specify/feature.json` contains
`{"feature_directory": "specs/005-consolidate-domain-logic"}`. It is gitignored,
so a merge does not carry it and a fresh clone will not have it. Fix it if it
points elsewhere.

**Spec Kit's scripts are unreliable here.** `setup-tasks.sh` and
`check-prerequisites.sh` resolve `FEATURE_DIR` from the *git branch name*
against the *main checkout* — so both report `specs/004-p3-workbench-shell` in
the wrong tree. Use `specs/005-consolidate-domain-logic/` inside the worktree
explicitly and ignore what those scripts say.

## Orient first

Read, in this order, before taking any action:

1. `.specify/memory/constitution.md` — governs everything below
2. `specs/005-consolidate-domain-logic/spec.md`
3. `specs/005-consolidate-domain-logic/plan.md`
4. `specs/005-consolidate-domain-logic/research.md` — **D1, D3, and D4 are your
   whole brief**
5. `specs/005-consolidate-domain-logic/data-model.md` — the range rules and the
   triage-record shape
6. `specs/005-consolidate-domain-logic/tasks.md` — your task list
7. `specs/004-p3-workbench-shell/sessions/handoff.md` §S1 — **read surprise 1
   about Node 24 and `localStorage` before writing a single test**

## Your scope: T001 through T008

The judgment half of this feature.

- **T001** — Verify the starting counts. Not assume them.
- **T002–T004** — US1: the three view-state defects, tests first.
- **T005** — Enumerate all 78 cases with decisions blank.
- **T006–T008** — Triage all 78, filling in decisions.

**Do not start T009.** T009–T021 re-home the survivors, delete the old files,
and verify. S2 owns that, and starting it here means finishing it badly.

## What makes this session different

Its output is decisions, not code. Exactly one source file is edited
(`src/store/viewState.ts`); everything else you produce is a record. The failure
mode is silent — a wrong triage call still leaves a green suite — so
`triage-record.md` is the deliverable. "I looked at them all" is not a
substitute for a row per case.

T007 is 47 individual judgment calls. Take them one at a time. A wholesale
decision about a group of cases is the thing this feature exists to prevent.

## Halt conditions

Halting is a normal, correct outcome. Stop and report if:

- **T001 or T005** finds a count that is not 47 / 27 / 4 / 78. The numbers in
  004's artifacts were already wrong once — they said 52 and 79 — so a mismatch
  is a real signal, not a rounding error.
- **T007** finds a case asserting both surviving and departing behavior in one
  block that you cannot split.
- Anything in `src/engine/` looks like it needs touching. It does not.
- The suite goes red for a reason you cannot explain.

## Rules that bind you

- **Constitution I** — `src/engine/` is not read or written. Not once.
- **Constitution II** — tests before implementation. Run T002's tests against
  the *unmodified* module and confirm each fails for its stated reason before
  writing T003. Dispatch `test-quality-reviewer` at T004.
- **Constitution IV** — no unbounded loops.
- **Constitution V** — `erasableSyntaxOnly`. `as const` objects with derived
  union types, never enums or namespaces.
- **Do not edit `specs/004-p3-workbench-shell/`'s `tasks.md`, `plan.md`,
  `spec.md`, or `research.md`.** `~/.claude/hooks/halt-on-speckit-replan.sh`
  will halt you, and it is right to. 005's own `tasks.md` you may tick.
- **Git** — you are in a worktree, so you and your subagents may commit to
  `004-p3-workbench-shell`. Never `push`, `merge`, `rebase`, `reset --hard`, or
  delete a branch. You do not make the commit that closes the feature.
- **Orchestration** — you are an orchestrator. No code beyond a 1–5 line edit;
  dispatch to subagents with an explicit `model: sonnet`. **T007 earns Opus** —
  47 judgment calls is where a cheap wrong answer is expensive.

## GitHub issues

T001–T021 are mirrored as **#165–#185**, labeled `005-consolidate-domain-logic`.
Close each as its commits land on the feature branch, not at merge.

**Task IDs repeat per feature** — 004's `T001` is issue #140, a different task —
so always scope by label.

Your scope maps to **#165–#172**.

## Before you finish

1. Run the gate:
   ```
   timeout 180 pnpm exec tsc -b   > ./tmp/tsc.log  2>&1
   timeout 120 pnpm --silent lint > ./tmp/lint.log 2>&1
   timeout 120 pnpm --silent test > ./tmp/test.log 2>&1
   ```
   Read a log only if its command failed. Run the suite **twice** — 004's S1
   recorded a file that passed once and then failed reproducibly on re-run.

2. Create `specs/005-consolidate-domain-logic/sessions/handoff.md` with a `## S1`
   section covering:
   - tasks completed, with commit SHAs
   - the starting counts you verified, and how you verified them
   - the triage tally: how many re-targeted, how many deleted, and the bucket
     each survivor is headed for
   - **any case you found hard to classify, and how you decided** — this is the
     part S2 cannot reconstruct from the record alone
   - whether the four provisional buckets in `plan.md` survived contact, or
     whether the real clusters are different
   - anything you did not finish, and why

3. Commit the handoff.

## Report back

Close with a plain summary for the human reading it: what landed, what the
triage tally is, anything surprising, and whether S2 can start as written or
needs its scope adjusted. No JSON, no machine-readable verdict — nothing parses
this.
