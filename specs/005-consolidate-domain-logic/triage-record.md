# Triage Record

One row per test case carried over from the layout that `004`'s workbench
shell replaces. Column meanings, the invariants each row must satisfy, and the
verified source counts live in
[`data-model.md`](./data-model.md) §Triage record — this file does not restate
them. `Decision` and `Destination / reason` are left blank here; T006–T008
fill them in.

Rows are numbered within each source file so the tally is countable at a
glance: 47 + 27 + 4 = 78.

---

## `__tests__/components/KitchenSinkPage.test.tsx` (47)

| # | Source (describe block) | Case | Decision | Destination / reason |
|---:|---|---|---|---|
| 1 | KitchenSinkPage render tests | renders without crashing | deleted | Asserts that `KitchenSinkPage` mounts, using `ActionButtons`' Generate Schedule button as its liveness probe. Both the page and `ActionButtons` leave the product at 004's T020, and a "the page mounts" smoke check has no subject once there is no page. |
| 2 | KitchenSinkPage render tests | renders tournament type dropdown | re-targeted | `__tests__/components/configEditing.test.tsx`, mounts `TournamentSetup`. The `tournament-type` select-trigger id it queries is `TournamentSetup.tsx`'s own, and the workbench keeps the control. |
| 3 | KitchenSinkPage render tests | renders days input | re-targeted | `__tests__/components/configEditing.test.tsx`, mounts `TournamentSetup`. The `days-available` select-trigger id is also `TournamentSetup.tsx`'s own but a different control from row 2's, so the two assertions survive independently. |
| 4 | KitchenSinkPage render tests | renders strips input | re-targeted | `__tests__/components/configEditing.test.tsx`, mounts `StripSetup`. `aria-label="Number of strips"` is on `StripSetup.tsx`'s own NumberInput. |
| 5 | KitchenSinkPage render tests | renders video strips input | re-targeted | `__tests__/components/configEditing.test.tsx`, mounts `StripSetup`. A second NumberInput in the same component, bounded by `max={stripsTotal}` — a distinct control from row 4's, not a restatement of it. |
| 6 | KitchenSinkPage render tests | renders template selector | deleted | Asserts that `TemplateSelector` renders: its ToggleGroupItem is the only `role="radio"` named "RYC Weekend" on the page. `TemplateSelector.tsx` is deleted by 004's T020. A template picker does survive inside `CompetitionMatrix` (a "Presets…" collapsible over the same `TEMPLATE_NAMES`, calling the same `applyTemplate`), but it is collapsed by default so this case's selector does not describe it — see report for the render-coverage gap that leaves. |
| 7 | KitchenSinkPage render tests | renders no Validate button — findings derive on every render | deleted | Asserts the absence of a Validate button from the page's action area. `ActionButtons` is the only component that ever hosted an action button and it leaves with the page. Re-mounting the assertion on `AnalysisOutput`, which never rendered such a button, would be vacuous — it would stay green even if a workbench regression added a Validate button elsewhere. The substantive half of the same product contract, that findings appear with no run in between, is re-targeted as rows 11, 20 and 28 against `AnalysisOutput`. |
| 8 | KitchenSinkPage render tests | renders Generate Schedule button | deleted | Asserts the presence of `ActionButtons`' Generate Schedule control, which leaves the product. Only its presence is asserted, not what it triggers, so the "re-target the trigger" rule does not apply — and `runScheduleAll()`'s surviving trigger, `ScheduleView`'s Regenerate button, already has its presence and effect covered by WizardShell row 26. |
| 9 | KitchenSinkPage render tests | renders Save to File button | re-targeted | `__tests__/components/saveLoadShare.test.tsx`, mounts `SaveLoadShare`. The Save to File button is `SaveLoadShare.tsx`'s own. |
| 10 | KitchenSinkPage render tests | renders Generate Link button | re-targeted | `__tests__/components/saveLoadShare.test.tsx`, mounts `SaveLoadShare`. Also `SaveLoadShare.tsx`'s own but on the share branch rather than row 9's download branch. |
| 11 | KitchenSinkPage render tests | shows findings on first render with no validate run | re-targeted | `__tests__/components/analysisOutput.test.tsx`, mounts `AnalysisOutput`. Both the Validation heading and the `strips_total must be > 0` text are `AnalysisOutput.tsx`'s own rendering of `selectDerivedFindings`, and this case is where the derive-on-render contract is asserted positively. |
| 12 | KitchenSinkPage render tests | shows the analysis empty state when the inputs raise nothing | re-targeted | `__tests__/components/analysisOutput.test.tsx`, mounts `AnalysisOutput`. "Nothing to report for the current inputs." is `AnalysisOutput.tsx`'s own empty-state string, reached through its `hasContent` branch. |
| 13 | KitchenSinkPage render tests | shows the schedule empty state before anything is placed | deleted | Exact coverage duplicate of WizardShell row 24's `ScheduleOutput` half, already re-targeted to `scheduleOutput.test.tsx`. Both mount `ScheduleOutput` with an empty placements map — `selectDerivedSchedule` returns `events: {}` in each — and assert the same "No events placed yet." string. The setups differ only in config values the empty-state branch never reads. |
| 14 | KitchenSinkPage render tests | shows empty fencer counts message when no competitions selected | re-targeted | `__tests__/components/configEditing.test.tsx`, mounts `FencerCounts`. "Select competitions above to enter fencer counts." is `FencerCounts.tsx`'s own zero-selection branch. |
| 15 | KitchenSinkPage render tests | renders competition toggles when template is applied | re-targeted | `__tests__/components/configEditing.test.tsx`, mounts `CompetitionMatrix`. The `aria-pressed` buttons it counts are `CompetitionMatrix.tsx`'s own `EventRow` buttons, and `applyTemplate` is invoked as a store action rather than through a deleted control, so nothing here depends on `TemplateSelector`. |
| 16 | KitchenSinkPage render tests | renders file input for loading configurations | re-targeted | `__tests__/components/saveLoadShare.test.tsx`, mounts `SaveLoadShare`. The hidden `input[type="file"]` is `SaveLoadShare.tsx`'s own, and every load case in that file drives it. |
| 17 | KitchenSinkPage user flow tests | selecting a template checks competition toggles | deleted | Renders the page but asserts only store state — `Object.keys(selectedCompetitions)` length after `applyTemplate`. `__tests__/store/store.test.ts`'s `applyTemplate > selects competitions from a named template` asserts the same thing strictly more tightly: `Object.keys(...).sort()` toEqual the full sorted template id list, pinning identity as well as count. |
| 18 | KitchenSinkPage user flow tests | selecting a template shows fencer count inputs | re-targeted | `__tests__/components/configEditing.test.tsx`, mounts `FencerCounts`. The `/Fencer count for/` spinbuttons are `FencerCounts.tsx`'s own per-competition rows; `applyTemplate` runs as a store action, so the case is single-component despite its name. |
| 19 | KitchenSinkPage user flow tests | entering fencer counts updates the inputs | re-targeted | `__tests__/components/configEditing.test.tsx`, mounts `FencerCounts`. Asserts the input's own displayed value after a change event — the render side of `FencerCounts.tsx`'s NumberInput, distinct from row 27, which asserts the store write. |
| 20 | KitchenSinkPage user flow tests | analysis output follows an input change with no run in between | re-targeted | `__tests__/components/analysisOutput.test.tsx`, mounts `AnalysisOutput`. Not cross-section despite the wording: the input change is a direct `setStrips(0)` store call, and both assertions — "Nothing to report" disappearing and the Validation heading appearing — are `AnalysisOutput.tsx`'s own. No composed host is needed. |
| 21 | KitchenSinkPage user flow tests | validation errors appear when strips is 0 with competitions selected | deleted | Exact duplicate of row 43: identical setup (`applyTemplate('RYC Weekend')`), identical render, identical single assertion on the Validation heading. Row 43 is kept as that assertion's home in `analysisOutput.test.tsx`. |
| 22 | KitchenSinkPage user flow tests | clicking Generate Schedule writes placements and renders the derived table | deleted | Clicks `ActionButtons`' Generate Schedule button, which leaves the product. The `runScheduleAll()` it triggers survives on `ScheduleView`'s Regenerate button, and WizardShell row 26 already covers it as a strict superset of this case: same seed, same `placements[id]` and rendered-id assertions, plus a pre-click check that placements start empty and the empty state renders. |
| 23 | KitchenSinkPage user flow tests | full flow: template -> strips -> fencer counts -> findings track every edit | re-targeted | `__tests__/components/configEditing.test.tsx`, composes a minimal host from `FencerCounts` + `AnalysisOutput`. Genuinely cross-section: the fencer-count edits are real `fireEvent.change` calls on `FencerCounts` inputs and the assertion lands on `AnalysisOutput`'s Validation heading. The host needs no `StripSetup` — the case drives strips through `setStrips(12)` directly. |
| 24 | KitchenSinkPage store integration tests | changing tournament type updates store state | deleted | Renders the page but asserts only store state; the case's own comment says the Radix Select cannot be driven by `fireEvent`, so it calls `setTournamentType` directly and the mount contributes nothing. `__tests__/store/store.test.ts`'s `setTournamentType > sets tournament_type` asserts the same action setting the same field. |
| 25 | KitchenSinkPage store integration tests | changing days input updates store state | deleted | Same shape as row 24 for `setDays`. Covered by `__tests__/store/store.test.ts`'s `setDays > sets days_available and initializes dayConfigs with default times`, which asserts the same field plus the `dayConfigs` seeding this case does not check. The day value differs (2 vs 4), which constrains nothing extra. |
| 26 | KitchenSinkPage store integration tests | changing strips input updates store state | re-targeted | `__tests__/components/configEditing.test.tsx`, mounts `StripSetup`. Unlike rows 24–25 this one drives the real control — change then blur on `StripSetup.tsx`'s NumberInput — so it covers the component's commit-on-blur wiring into `setStrips`, which no store test reaches. |
| 27 | KitchenSinkPage store integration tests | changing fencer count input updates store state | re-targeted | `__tests__/components/configEditing.test.tsx`, mounts `FencerCounts`. Drives the real NumberInput and asserts the resulting `updateCompetition` write, covering `FencerCounts.tsx`'s per-id onChange wiring rather than the displayed value row 19 checks. |
| 28 | KitchenSinkPage store integration tests | applying a template immediately changes what the analysis shows | re-targeted | `__tests__/components/analysisOutput.test.tsx`, mounts `AnalysisOutput`. `TemplateSelector` is deleted but `applyTemplate` survives as a store action and this case already calls it directly, so every DOM assertion is on `AnalysisOutput`'s empty-state branch. Distinct from row 20: it reaches the findings through the analysis path (18 new events) rather than the strip-count validation path. |
| 29 | SaveLoadShare save tests | clicking Save to File triggers URL.createObjectURL | re-targeted | `__tests__/components/saveLoadShare.test.tsx`, mounts `SaveLoadShare`. Asserts `handleSave`'s own effects — one `URL.createObjectURL`, one anchor click, `download` of `tournament.piste.json`. |
| 30 | SaveLoadShare save tests | saved JSON carries the v2 shape including placements | re-targeted | `__tests__/components/saveLoadShare.test.tsx`, mounts `SaveLoadShare`. Not an exact duplicate of `__tests__/store/serialization.test.ts`: those 76 cases exercise `serializeState` against a state object, while this one pins the wiring — that the blob handed to `URL.createObjectURL` on click is the serialization of the live store. Its v2-shape assertions are redundant with serialization.test.ts, but the load-bearing claim has no other home. |
| 31 | SaveLoadShare load tests | loading valid JSON hydrates store state | re-targeted | `__tests__/components/saveLoadShare.test.tsx`, mounts `SaveLoadShare`. Exercises the file-input change handler, FileReader and `useStore.setState` path. `deserializeState` itself is covered in serialization.test.ts; the component's plumbing into it is not. |
| 32 | SaveLoadShare load tests | loading a placement whose event is not selected reports the drop | re-targeted | `__tests__/components/saveLoadShare.test.tsx`, mounts `SaveLoadShare`. The `role="status"` notice and its "Dropped 1 placement" wording are `SaveLoadShare.tsx`'s own rendering of `result.droppedPlacements`. |
| 33 | SaveLoadShare load tests | loading a file with no orphan placements shows no drop notice | re-targeted | `__tests__/components/saveLoadShare.test.tsx`, mounts `SaveLoadShare`. The negative counterpart to row 32, and the only case pinning the "live region stays mounted while empty" detail the component's own comment calls out. |
| 34 | SaveLoadShare load tests | loading valid JSON clears any previous load error | re-targeted | `__tests__/components/saveLoadShare.test.tsx`, mounts `SaveLoadShare`. Asserts a component state transition — `loadError` reset on a subsequent good load — not serialization behavior. |
| 35 | SaveLoadShare load tests | loading invalid JSON shows error message | re-targeted | `__tests__/components/saveLoadShare.test.tsx`, mounts `SaveLoadShare`. Asserts the exact "Invalid JSON" string reaching the `role="alert"` paragraph. |
| 36 | SaveLoadShare load tests | loading JSON with wrong schema shows error message | re-targeted | `__tests__/components/saveLoadShare.test.tsx`, mounts `SaveLoadShare`. A different branch from row 35 — schema rejection ("Unknown top-level field") rather than a parse failure — proving the component surfaces validator messages verbatim, not only its own. |
| 37 | KitchenSinkPage error state tests | malformed file upload shows error message | deleted | Exact coverage duplicate of row 35. `'malformed!!!content'` and `'{ this is not valid json }'` both fail `JSON.parse` inside `deserializeState` and render the same "Invalid JSON" string, and this case's `/invalid\|malformed\|error\|parse\|json/i` assertion is satisfied by exactly the string row 35 pins literally. Same component, same branch, same output, strictly weaker assertion — nothing is lost. |
| 38 | KitchenSinkPage error state tests | Generate Schedule is disabled while derived findings hold a hard error | deleted | Asserts the `disabled` state of `ActionButtons`' Generate Schedule button. `ActionButtons` leaves the product and no surviving component implements the gate — `ScheduleView`'s Regenerate button has no `disabled` prop — so there is nothing to mount it against. Checked before deciding: `__tests__/store/derived.test.ts`'s `selectDerivedFindings > reports validation errors from current inputs` covers the finding surfacing but keys on `field === 'strips_total'`, not `severity === 'ERROR'`, so the severity half of the gate contract leaves the suite uncovered. Flagged in the report for the workbench's own gating task. |
| 39 | KitchenSinkPage error state tests | Generate Schedule is enabled once the hard error is fixed | deleted | The same departing control as row 38, in its enabled direction. Its recompute-on-fix half needs no new home: `derived.test.ts`'s `selectDerivedFindings > reports validation errors from current inputs – a zero strip count surfaces and clears` runs the identical zero-strips-then-fixed sequence against the same selector this button's `disabled` prop read — the coverage WizardShell row 13 was decided on. |
| 40 | KitchenSinkPage error state tests | schedule output renders a row derived from a seeded placement | deleted | Exact duplicate of WizardShell row 23, already re-targeted to `scheduleOutput.test.tsx`. That case's `seedPlacedCompetition()` helper is line-for-line this case's inline setup, and the three assertions — rendered competition id, an "8:00" cell, absent empty state — are identical. |
| 41 | KitchenSinkPage error state tests | a placement on a day past days_available is flagged, not hidden | re-targeted | `__tests__/components/scheduleOutput.test.tsx`, mounts `ScheduleOutput`. The "Day 8 out of range" badge is `ScheduleOutput.tsx`'s own `day_out_of_range` rendering. `derived.test.ts` flags the selector field, but nothing else asserts the row is rendered flagged rather than filtered out. |
| 42 | KitchenSinkPage error state tests | Generate Link produces URL containing #config= hash | re-targeted | `__tests__/components/saveLoadShare.test.tsx`, mounts `SaveLoadShare`. The readonly share-URL input and its `#config=` value are `SaveLoadShare.tsx`'s own, and the assertion that exactly one `input[readonly]` exists still holds with the component mounted alone. |
| 43 | AnalysisOutput section | shows Validation heading when the inputs produce validation errors | re-targeted | `__tests__/components/analysisOutput.test.tsx`, mounts `AnalysisOutput`. Kept as the home for the Validation-heading assertion that row 21 duplicated. |
| 44 | AnalysisOutput section | shows Warnings heading when the analysis raises warnings | re-targeted | `__tests__/components/analysisOutput.test.tsx`, mounts `AnalysisOutput`. A separate branch of `AnalysisOutput.tsx` from the Validation one, reached only when `analysis.warnings` is non-empty. |
| 45 | AnalysisOutput section | shows Flighting Suggestions heading with Accept/Reject buttons | re-targeted | `__tests__/components/analysisOutput.test.tsx`, mounts `AnalysisOutput`. The heading and the pending-state Accept/Reject buttons are `AnalysisOutput.tsx`'s own; it is the only case asserting the pending state renders buttons rather than a badge. |
| 46 | AnalysisOutput section | clicking Accept changes suggestion state to Accepted | re-targeted | `__tests__/components/analysisOutput.test.tsx`, mounts `AnalysisOutput`. Asserts both the Accepted badge and the `flightingSuggestionStates[0]` write — component-to-store wiring for `acceptFlightingSuggestion`, which store.test.ts covers only at the action level. |
| 47 | AnalysisOutput section | clicking Reject changes suggestion state to Rejected | re-targeted | `__tests__/components/analysisOutput.test.tsx`, mounts `AnalysisOutput`. The reject branch renders a different Badge variant and writes a different state value, so row 46 does not cover it. |

## `__tests__/components/WizardShell.test.tsx` (27)

| # | Source (describe block) | Case | Decision | Destination / reason |
|---:|---|---|---|---|
| 1 | WizardShell navigation | renders step 1 (Tournament) by default | deleted | Asserts the wizard's default step-1 rendering; the workbench has no step sequence to default into. |
| 2 | WizardShell navigation | Forward button advances to the next step | deleted | Asserts Forward-button step advancement; the workbench has no Forward button or step sequence. |
| 3 | WizardShell navigation | Back button retreats to the previous step | deleted | Asserts Back-button step retreat; the workbench has no Back button or step sequence. |
| 4 | WizardShell navigation | Back button is disabled on Step 1 (index 0) | deleted | Asserts Back-button disabled state tied to step index; the workbench has no Back button. |
| 5 | WizardShell navigation | Back button is enabled when not on Step 1 | deleted | Asserts Back-button enabled state tied to step index; the workbench has no Back button. |
| 6 | WizardShell navigation | clicking Back on Step 1 does not go below step 0 | deleted | Asserts a step-index floor on the Back button; the workbench has no Back button or step index. |
| 7 | WizardShell navigation | Forward button shows "Next" on steps 0–2 | deleted | Asserts Forward-button label switching by step; the workbench has no Forward button or steps. |
| 8 | WizardShell navigation | Forward button shows "View Schedule" on Step 4 (Analysis, index 3) | deleted | Asserts Forward-button label switching by step; the workbench has no Forward button or steps. |
| 9 | WizardShell navigation | Forward button is not rendered on Step 5 (Schedule, index 4) | deleted | Asserts Forward-button presence tied to step index; the workbench has no Forward button or steps. |
| 10 | WizardShell navigation | Forward blocked on Step 4 (Analysis) when hard ERROR validation errors exist | deleted | Asserts Forward-button disabled state gated on hard validation errors; the workbench has no Forward button. Hard-error detection itself is covered independently (e.g. KitchenSinkPage error-state tests, AnalysisOutput's Validation heading tests). |
| 11 | WizardShell navigation | Forward blocked on Step 4 does not advance step when clicked | deleted | Asserts that a disabled Forward button does not advance the step on click; the workbench has no Forward button or step index. |
| 12 | WizardShell navigation | Forward allowed on Step 4 when no hard errors | deleted | Asserts Forward-button enabled state when no hard errors; the workbench has no Forward button. |
| 13 | WizardShell navigation | fixing strips re-enables Forward without any validate run | deleted | Its navigation half (Forward-button disabled/enabled state) leaves with the Forward button. Its derived-findings half (validation recomputes on setStrips with no explicit validate call) needs no new home: it is already covered by `__tests__/store/derived.test.ts`'s `selectDerivedFindings > reports validation errors from current inputs – a zero strip count surfaces and clears`, which exercises the identical zero-strips-then-fixed sequence against the same selector this button's `disabled` prop reads. See report for the full reasoning. |
| 14 | WizardShell navigation | step indicator renders all 5 step labels | deleted | Asserts the step-indicator chrome; the workbench has no step indicator. |
| 15 | Layout toggle | default layout mode is wizard | deleted | Asserts the default `layoutMode` value; the wizard/kitchen-sink toggle leaves the product at 004's T020. The `layoutMode` field itself persists dormant in the store until then (research D6) but the toggle behavior it drives does not. |
| 16 | Layout toggle | switching to wizard layout renders wizard content | deleted | Asserts the layout toggle switching to the wizard view; the toggle and the wizard both leave the product. |
| 17 | Layout toggle | switching to kitchen-sink layout hides wizard content | deleted | Asserts the layout toggle switching to the kitchen-sink view; the toggle and the wizard both leave the product. |
| 18 | Layout toggle | wizard layout renders WizardShell step indicator labels | deleted | Asserts the layout toggle plus the step indicator it reveals; both leave the product. |
| 19 | Layout toggle | kitchen-sink layout does not render wizard step indicators | deleted | Asserts the layout toggle plus the step indicator's absence in the other mode; both leave the product. |
| 20 | Layout toggle | state (strips_total) is preserved when switching layouts | deleted | Asserts that toggling `layoutMode` does not reset unrelated store state (`strips_total`). The toggle itself is removed, so there is no longer a layout switch for any state to be preserved across. |
| 21 | Layout toggle | wizard step is preserved when switching layouts | deleted | Asserts that toggling `layoutMode` does not reset `wizardStep`. Both the toggle and the wizard-step slice's product behavior leave with the wizard. |
| 22 | ScheduleView derived output | renders no staleness banner — placements are always current | re-targeted | `__tests__/components/scheduleOutput.test.tsx`, mounts `ScheduleOutput`. Pure negative rendering check against `ScheduleOutput.tsx`, which owns the schedule-rows display. |
| 23 | ScheduleView derived output | a placement seeded into the store renders as a schedule row | re-targeted | `__tests__/components/scheduleOutput.test.tsx`, mounts `ScheduleOutput`. Asserts only `ScheduleOutput.tsx`'s own row rendering (competition id, pool-start time) and its own empty-state text. |
| 24 | ScheduleView derived output | shows the empty state when nothing is placed | re-targeted | Splits across two single-component destinations, not genuinely cross-section: `__tests__/components/scheduleOutput.test.tsx` mounting `ScheduleOutput` for "No events placed yet.", and `__tests__/components/refRequirementsReport.test.tsx` mounting `RefRequirementsReport` for "No referee demand — nothing is placed yet." Neither text depends on the other component being present — the original case bundled two independent single-component empty states because it rendered the composite `ScheduleView`. See report. |
| 25 | ScheduleView derived output | referee requirements derive from the placement, not from a scheduler run | re-targeted | `__tests__/components/refRequirementsReport.test.tsx`, mounts `RefRequirementsReport`. Deviates from research D1's cluster table, which lists `ScheduleOutput` for "referee figures deriving from placements" — but `ScheduleOutput.tsx` renders no referee text at all ("Referee Requirements", "Peak Total Refs", "No referee demand..." are all `RefRequirementsReport.tsx`'s own strings). `RefRequirementsReport` is the actual smallest surviving component exhibiting this behavior, consistent with D1's own rule if not its summary table. Flagged for reconciliation — see report. |
| 26 | ScheduleView derived output | Regenerate writes placements and the derived table follows | re-targeted | `__tests__/components/scheduleView.test.tsx`, mounts `ScheduleView`. The Regenerate button and its `runScheduleAll()` call are defined directly in `src/components/ScheduleView.tsx`, which is not under `src/components/sections/` and is not among 004 T020's deletions (research D6 names only `KitchenSinkPage.tsx` and the 5 files under `wizard/`) — so it remains the smallest surviving component containing this behavior. This is a gap in D1's cluster table, which addresses only `ActionButtons`/`TemplateSelector` as deleted triggers; `ScheduleView.tsx`'s own inline button is a third, uncovered case. Flagged for reconciliation — see report. |
| 27 | ScheduleView derived output | editing a placement changes the rendered schedule with no re-run | re-targeted | `__tests__/components/scheduleOutput.test.tsx`, mounts `ScheduleOutput`. Asserts only `ScheduleOutput.tsx`'s own row rendering following a `updatePlacement` call. |

## `__tests__/store/store.test.ts` — `describe('uiSlice')` (4)

| # | Source (describe block) | Case | Decision | Destination / reason |
|---:|---|---|---|---|
| 1 | uiSlice > initial state | has correct defaults | deleted | Asserts the default values of `layoutMode` and `wizardStep`. The layout toggle and the wizard's step sequence leave the product at 004's T020, even though both fields persist dormant in the store until then (research D6). |
| 2 | uiSlice > setLayoutMode | sets layout mode to wizard | deleted | Asserts `setLayoutMode('wizard')`; the layout toggle leaves the product with the wizard/kitchen-sink distinction it switches between. |
| 3 | uiSlice > setLayoutMode | sets layout mode to kitchen-sink | deleted | Asserts `setLayoutMode('kitchen-sink')`; the layout toggle leaves the product with the wizard/kitchen-sink distinction it switches between. |
| 4 | uiSlice > setStep | sets wizardStep | deleted | Asserts `setStep`; wizard step navigation leaves the product with the wizard. |
