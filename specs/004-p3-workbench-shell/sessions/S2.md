You are executing session **S2** of feature `004-p3-workbench-shell` in the
Piste Planner repository. You have no memory of earlier sessions. A previous
session did T001–T005 and left you a record.

## Orient first

Read, in this order, before taking any action:

1. `specs/004-p3-workbench-shell/sessions/handoff.md` — **read this first.**
   S1 wrote what it found and what you need to know.
2. `git log --oneline main..HEAD` — what actually landed, as opposed to what
   was planned
3. `.specify/memory/constitution.md`
4. `specs/004-p3-workbench-shell/spec.md` — User Story 1 in particular
5. `specs/004-p3-workbench-shell/plan.md`
6. `specs/004-p3-workbench-shell/contracts/ui-contract.md` — §Regions is the
   contract you are building to
7. `specs/004-p3-workbench-shell/tasks.md`

You are already inside the git worktree for branch `004-p3-workbench-shell`.

## Your scope: T006 through T019

Build the workbench shell and mount the existing schedule table in its center.

- **T006–T010** — the failing tests, first. Write them, run them, confirm each
  fails for the reason it is supposed to.
- **T011–T015** — the four regions plus the unplaced tray.
- **T016** — re-point `ScheduleOutput` at derived placements as the center
  content.
- **T017** — the dimmed-invalid overlay.
- **T018** — two-tier recompute.
- **T019** — boot with a preset loaded and auto-scheduled.

Stop at T019. **Do not start T020.** T020 deletes the wizard and the kitchen
sink, and T021–T022 confirm the already-triaged tests behind it still pass
against what you build here — that context does not belong in this session
either.

## What you are building against

Deliberately, at the end of this session both old layouts still exist and still
work. You are adding the workbench beside them, not replacing them yet. The
replacement happens in S3.

The center content in this story is the **existing schedule table**, not the
matrix. The matrix is US2 and a later session. Do not build canvas code here,
and do not add a view toggle — there is only one view until US2 lands.

Do not add a placeholder for the gears control in the top bar. US5 adds it. A
placeholder is a thing a later session has to find and remove.

## Rules that bind you

- **Constitution I** — `src/engine/` is pure and this session does not touch it.
  `buildConfig.ts` is the only bridge from store to engine.
- **Constitution II** — tests before implementation, confirmed failing first.
  Dispatch `test-quality-reviewer` after tests, `react-code-reviewer` after
  React work. Both are in your task list; do not skip them.
- **Constitution III** — this session must produce **zero** engine drift. Only
  US4 is allowed to change engine output. If the B1–B8 numbers move, you have a
  bug in this session's work, not a drift to record. The baseline is in
  `specs/004-p3-workbench-shell/drift-baseline.md`.
- **Constitution IV** — no unbounded loops.
- **Constitution V** — `erasableSyntaxOnly`. `as const` objects with derived
  union types.
- **Git** — worktree, so you and your subagents commit to
  `004-p3-workbench-shell`. **Never** `push`, `merge`, `rebase`, `reset --hard`,
  or delete a branch. You do not make the commit that closes the feature.
- **Orchestration** — you are an orchestrator. No code beyond a 1–5 line edit;
  dispatch to subagents with an explicit `model: sonnet` unless the task is
  complicated.
- Tick checkboxes in `tasks.md` as you go. Do not otherwise restructure
  `tasks.md`, `plan.md`, or `spec.md` — a hook will halt you.

## Before you finish

1. Run the gate:
   ```
   timeout 180 pnpm exec tsc -b   > ./tmp/tsc.log  2>&1
   timeout 120 pnpm --silent lint > ./tmp/lint.log 2>&1
   timeout 120 pnpm --silent test > ./tmp/test.log 2>&1
   ```
   Read a log only on failure. The existing wizard and kitchen-sink tests must
   still pass — you have not deleted them yet.

2. Append a `## S2` section to
   `specs/004-p3-workbench-shell/sessions/handoff.md` covering:
   - tasks completed, with commit SHAs
   - the accessible names you gave every control you built. **S3 re-points
     `scripts/smoke.mjs` at these**, and every one of that driver's current
     locators targets UI that S3 deletes. If you do not write the names down,
     S3 rediscovers them by trial and error against a real browser.
   - which existing components you re-homed into the rail and anything that
     fought you
   - anything you did not finish, and why

3. Commit the handoff.

## Your final message

End your final message with exactly this JSON and nothing after it:

```json
{"status": "ok", "reason": "<one sentence>", "tasks_done": ["T006", "..."]}
```

Use `"status": "halted"` if you could not finish — a red gate you could not fix,
a task whose premise turned out to be wrong, engine drift you did not cause
deliberately, or anything needing a human decision. A halt is a normal outcome.
Put the specifics in `reason` and in the handoff.
