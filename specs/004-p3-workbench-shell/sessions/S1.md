You are executing session **S1** of feature `004-p3-workbench-shell` in the
Piste Planner repository. You have no memory of earlier sessions. Everything you
need is on disk.

## Orient first

Read, in this order, before taking any action:

1. `.specify/memory/constitution.md` — governs everything below
2. `specs/004-p3-workbench-shell/spec.md`
3. `specs/004-p3-workbench-shell/plan.md`
4. `specs/004-p3-workbench-shell/research.md` — D5, D6, and D1 are the decisions
   that shaped this feature
5. `specs/004-p3-workbench-shell/tasks.md` — your task list

You are already inside the git worktree for branch `004-p3-workbench-shell`,
created by `scripts/run-chain.sh` before you started. Do not create a worktree
or a branch. The feature's spec artifacts are already tracked — they landed on
`main` in commit 60d7fd39df, so this checkout carries them and there is nothing
to import.

## Your scope: T001 through T005

Execute exactly these, in order, and stop:

- **T001** — Confirm and record the starting point: on the right branch, clean
  working tree, artifacts present, and the merge-base with `main` noted for
  T002. If the working tree is dirty, stop and say so rather than committing
  whatever you find — it would mean uncommitted work leaked in from elsewhere.
- **T002** — Capture the pre-change B1–B8 drift baseline into
  `specs/004-p3-workbench-shell/drift-baseline.md`, **before any source file is
  edited**. This is the number the US4 drift gate compares against much later,
  and it is only valid if taken against untouched `main`.
- **T003** — Write failing tests for the view-state module.
- **T004** — Implement the view-state module.
- **T005** — Dispatch `test-quality-reviewer` on T003's tests.

Do not start T006 or anything beyond it. A later session owns that.

## Notes on T002

The baseline needs, per scenario B1 through B8: scheduled event count, peak pool
referee demand, and peak DE referee demand. The pieces you need:

- `__tests__/helpers/scenarios.ts` exports `SCENARIOS`, `buildCompetitions`,
  and `tournamentConfig`
- `scheduleAll` is in `src/engine/scheduler.ts`
- `computeRefRequirements` is in `src/engine/refs.ts`
- `__tests__/engine/integration.test.ts` shows how the scenarios are driven, and
  honors `PISTE_VISUALIZE=1`

Record the numbers in a table, note the commit SHA of `main` they were taken
against, and say plainly which scenarios research D5 and D6 predict will move
later, so the session that runs the gate knows what it is looking for.

## Rules that bind you

- **Constitution I** — `src/engine/` is pure. Nothing in this session touches it.
- **Constitution II** — tests before implementation. Run T003's tests and
  confirm they fail for the stated reason before writing T004.
- **Constitution IV** — no unbounded loops.
- **Constitution V** — `erasableSyntaxOnly`. `as const` objects with derived
  union types, never enums or namespaces.
- **Git** — you are in a worktree, so you and your subagents may commit to
  `004-p3-workbench-shell`. You must **never** run `git push`, `git merge`,
  `git rebase`, `reset --hard`, or delete a branch. You do not make the commit
  that closes the feature.
- **Orchestration** — you are an orchestrator. Do not write code directly beyond
  a 1–5 line edit; dispatch to subagents. Coding subagents get an explicit
  `model: sonnet` unless the task is complicated.
- Tick each task's checkbox in `tasks.md` as you complete it. Checkbox-only
  edits are fine; do not otherwise restructure `tasks.md`, `plan.md`, or
  `spec.md` — a hook will halt you if you do.

## Before you finish

1. Run the gate and confirm it is green:
   ```
   timeout 180 pnpm exec tsc -b   > ./tmp/tsc.log  2>&1
   timeout 120 pnpm --silent lint > ./tmp/lint.log 2>&1
   timeout 120 pnpm --silent test > ./tmp/test.log 2>&1
   ```
   Read a log only if its command failed.

2. Append a section to `specs/004-p3-workbench-shell/sessions/handoff.md`
   (create it if absent) headed `## S1`, covering:
   - which tasks you completed, with their commit SHAs
   - the drift baseline numbers, repeated here so the next session does not have
     to open another file to see them
   - anything you found that the next session must know — a surprise in the
     code, a decision you made that the plan did not anticipate, a test that
     was already failing before you started
   - anything you did **not** finish, and why

3. Commit the handoff.

## Your final message

End your final message with exactly this JSON and nothing after it:

```json
{"status": "ok", "reason": "<one sentence>", "tasks_done": ["T001", "..."]}
```

Use `"status": "halted"` if you could not finish — a red gate you could not fix,
a task whose premise turned out to be wrong, or anything that needs a human
decision. A halt is a normal outcome and is far better than pressing on. Put the
specifics in `reason` and in the handoff.
