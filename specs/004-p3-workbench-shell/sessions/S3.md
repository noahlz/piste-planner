You are executing session **S3** of feature `004-p3-workbench-shell` in the
Piste Planner repository. You have no memory of earlier sessions. Previous
sessions did T001–T019 and left you a record.

**The coverage triage this session was written to run has already happened.**
Feature `005-consolidate-domain-logic` worked through all 78 cases in the two
departing layout test files, plus the 4 `uiSlice` cases in
`__tests__/store/store.test.ts`, while this branch was in flight: 39
re-targeted into four new behavior-named test files, 39 deleted.
`specs/005-consolidate-domain-logic/triage-record.md` is the row-by-row record,
with reasoning, of every one of those decisions. **This is still the session
most likely to lose something quietly** — not by mis-triaging a case, there is
none left to triage, but by deleting the source T020 touches without checking
that the already-triaged tests still pass against what's left. Read the whole
of this file before starting.

## Orient first

Read, in this order, before taking any action:

1. `specs/004-p3-workbench-shell/sessions/handoff.md` — **read this first.**
   S2's section lists the accessible names of every control it built. You need
   those for T023.
2. `git log --oneline main..HEAD`
3. `.specify/memory/constitution.md` — §VI especially
4. `specs/004-p3-workbench-shell/research.md` — D12 explains *why* tests are
   re-targeted by behavior rather than deleted wholesale; its case counts (52
   for KitchenSinkPage, 79 total) are superseded.
   `specs/005-consolidate-domain-logic/triage-record.md` is what actually
   happened — read it for the decisions themselves, not D12.
5. `specs/004-p3-workbench-shell/tasks.md`

You are already inside the git worktree for branch `004-p3-workbench-shell`.

## Your scope: T020 through T025

- **T020** — delete `src/components/wizard/` (5 files),
  `src/components/KitchenSinkPage.tsx`, `TemplateSelector.tsx`,
  `ActionButtons.tsx`, and the `layoutMode` slice and layout toggle. **This is
  now a source-only deletion.** Feature 005 already removed every test that
  referenced `WizardShell`, `KitchenSinkPage`, or `layoutMode` —
  `__tests__/components/KitchenSinkPage.test.tsx`, `WizardShell.test.tsx`, and
  the four `uiSlice` cases in `__tests__/store/store.test.ts` are gone. Confirm
  that with a grep before you delete, not after.
- **T021 / T022** — no triage left to do. The 78 cases from those two files
  were already decided by feature 005: 39 re-targeted into
  `__tests__/components/saveLoadShare.test.tsx` (12),
  `configEditing.test.tsx` (11), `analysisOutput.test.tsx` (9), and
  `scheduleOutput.test.tsx` (8) — 40 test cases total across the four files,
  because one WizardShell case split into two independent ones. The other 39
  were deleted as asserting navigation, layout switching, or composition that
  no longer exists. See "How to do T021 and T022 now" below for what these two
  tasks are actually for in this session.
- **T023** — re-point `scripts/smoke.mjs`.
- **T024** — `test-quality-reviewer`.
- **T025** — `react-code-reviewer`.

This closes the US1 checkpoint. Stop there.

## How to do T021 and T022 now

There is no case-by-case judgment left here — that was feature 005's job, and
it is finished. Do not re-decide anything in
`specs/005-consolidate-domain-logic/triage-record.md`; treat its `Decision` and
`Destination / reason` columns as settled.

What's actually left:

1. Confirm `__tests__/components/KitchenSinkPage.test.tsx` and
   `__tests__/components/WizardShell.test.tsx` are gone, and that
   `grep -rn "KitchenSinkPage\|WizardShell\|layoutMode" __tests__/` returns
   nothing.
2. Run the four destination files —
   `__tests__/components/saveLoadShare.test.tsx`, `configEditing.test.tsx`,
   `analysisOutput.test.tsx`, and `scheduleOutput.test.tsx` — against the
   workbench as it stands after S2 and after your own T020 deletion. They were
   written mounting the section components directly (`TournamentSetup`,
   `StripSetup`, `SaveLoadShare`, and the rest), not through `KitchenSinkPage`
   or `WizardShell`, so nothing in T020 should touch them — confirm that rather
   than assume it.
3. If a mount now fails because S2 re-homed a component with a new required
   prop or a context provider it did not need before, fix the mount in place.
   That is the re-pointing T021/T022 are for now — not deciding what survives,
   only keeping an already-decided test passing against the UI it describes.
4. If all four files already pass unchanged, say that plainly in the handoff.
   Finding nothing to fix is a legitimate outcome here, not a sign you skipped
   a step.

Record in the handoff which of the four files, if any, needed a mount fix and
what the fix was.

## How to do T023

Every locator in `scripts/smoke.mjs` currently targets UI that T020 deletes —
the `Single Page` tab, the `Wizard` tab, `Generate Schedule`,
`Save / Load / Share`, `Suggest`, `Number of strips`, `Generate Link`, and the
fencer-count spinbutton.

**Constitution VI forbids rewriting it from scratch.** Its selectors are the
accumulated record of corrections made against the real DOM — the header
comment says the picker is a ToggleGroup not a select, that "Number of strips"
matches three elements unless scoped by role, that the share button reads
"Generate Link". Rewriting discards that record and rediscovers the same
failures.

So: re-point each step at the workbench control that replaces the one it drove,
using the accessible names S2 recorded in the handoff. Keep the header comment's
corrections wherever the selector they describe survives in a new form, and
delete the wizard walk at the end of the file, which has no replacement.

Then run it against the live app and make it pass:

```
pnpm dev &
timeout 120 pnpm --silent smoke > ./tmp/smoke.log 2>&1
```

**Dispatch the smoke repair to a subagent.** Locator repair runs in rounds of
four or five full round trips, and paying your own context for each one is what
the constitution's orchestration section warns against. Let the subagent iterate
and report the verdict back.

## Rules that bind you

- **Constitution III** — zero engine drift this session. Only US4 changes engine
  output. Baseline is in `specs/004-p3-workbench-shell/drift-baseline.md`.
- **Constitution VI** — `scripts/smoke.mjs` is a repo artifact, repaired in
  place. Never a scratch file.
- **Git** — worktree, so commits to `004-p3-workbench-shell` are fine.
  **Never** `push`, `merge`, `rebase`, `reset --hard`, or delete a branch. You
  do not make the commit that closes the feature.
- **Orchestration** — no code beyond a 1–5 line edit; dispatch to subagents,
  `model: sonnet` unless the task is complicated.
- Tick checkboxes in `tasks.md`. Do not otherwise restructure `tasks.md`,
  `plan.md`, or `spec.md` — a hook will halt you.

## Before you finish

1. Run the gate:
   ```
   timeout 180 pnpm exec tsc -b   > ./tmp/tsc.log  2>&1
   timeout 120 pnpm --silent lint > ./tmp/lint.log 2>&1
   timeout 120 pnpm --silent test > ./tmp/test.log 2>&1
   ```

2. Confirm no test anywhere in the suite still references `WizardShell`,
   `KitchenSinkPage`, or `layoutMode`.

3. Confirm `scripts/smoke.mjs` passes against the running app.

4. Append a `## S3` section to
   `specs/004-p3-workbench-shell/sessions/handoff.md` covering:
   - tasks completed, with commit SHAs
   - for T021/T022: which of the four already-triaged test files, if any,
     needed a mount fix against S2's regions, and what the fix was — the
     re-target/delete tally itself is already recorded in
     `specs/005-consolidate-domain-logic/triage-record.md` and does not need
     restating
   - **any coverage you knowingly dropped, and why.** This is the thing most
     worth writing down and the easiest to leave out.
   - which smoke locators you re-pointed and which you deleted
   - anything you did not finish, and why

5. Commit the handoff, then make the US1 checkpoint commit.

## Your final message

End your final message with exactly this JSON and nothing after it:

```json
{"status": "ok", "reason": "<one sentence>", "tasks_done": ["T020", "..."]}
```

Use `"status": "halted"` if you could not finish. Specific reasons to halt here:
the smoke driver will not pass and you cannot see why, a T021/T022 mount fix
would require changing what a test asserts rather than just how it renders —
that is a re-triage decision, not yours to make solo, so flag it instead — or
you find yourself about to delete coverage you cannot justify. Halting is the
right call in every one of those. Put the specifics in `reason` and in the
handoff.
