# 013 session 10 – phase 7 (T036)

Paste into a fresh session started from the **main checkout**
`/Users/noahlz/projects/piste-planner`, not from a worktree.

---

Continue feature 013 (workbench redesign) at phase 7, T036.

**Check the state before trusting this file.** It was written at the phase 6
bookkeeping commit on branch `013-phase6-pins` in the worktree
`/Users/noahlz/projects/piste-planner-013-phase6-pins`. If that branch is
now merged into `main` and its worktree removed, everything below is on
`main` and the paths here are stale — `git log --oneline -5` settles it. If
it is **not** merged, stop: the user merges it first (`git merge --no-ff
--no-commit 013-phase6-pins`, the post-merge gate, then `commit-with-costs`).

Cut a fresh worktree off `main` for this session (never reuse a previous
session's):

```
git worktree add /Users/noahlz/projects/piste-planner-013-phase7-schedule \
    -b 013-phase7-schedule main
```

Worktree flow, per the constitution: subagents commit to that branch, the user
makes the merge commit. Then read `specs/013-workbench-redesign/tasks.md` and
`handoff.md` and execute with `/speckit-implement` from **T036**. Stop at the
phase 7 checkpoint after T036.

## State

Phase 6 closed 2026-09-14. `scheduleAll(competitions, config, pinned = [])`
schedules around pinned events: pins seed the day colouring, a pre-claim
pass claims their strip-time before the loop runs, the loop's seed skips
them, the strip search threads the same list, `runScheduleAll` keeps pins
verbatim and returns `{ placed, unplaced }` over the unpinned events. The
drift ledger did not move on any scenario (24/24/24/17/12/45/18/52, SHA
`5483c40c1349…`), read twice — by T034's implementer and by T035's second
reader.

Gate at `f072d754b5`: `tsc -b` and lint clean, 76 files / 1739 tests, ledger
and parity 35 passed. Run twice — follow-up implementer and orchestrator.

Commits, all on `013-phase6-pins`:

- `29f95362f9` T034 – four test files, `PinnedPlacement`, `PINNED_UNCLAIMED`,
  the seeded colouring, the pre-claim pass, the search rule,
  `buildPinnedPlacements`, `setPlacementsFromAuto(placements, keep)`.
- `dc1a1b073b` T035 – the after-table in `drift-baseline.md`.
- `f072d754b5` review follow-up – cases 4 and 7 reshaped to diverging pins
  (mutation-proven red), the catalogue-existence check in
  `buildPinnedPlacements` with its test.
- the bookkeeping commit – T033–T035 ticked, handoff findings 21–25, this
  file.

## Findings carried into phase 7 (all in handoff.md)

- **Findings 6 and 13** (owner decisions, still open): no control returns
  `de_mode_override` to null; a placed block is selectable by mouse only.
- **Finding 25**: `Canvas.tsx` still reads `placements` live for the pin
  badge (finding 11's remainder). No phase names it; T044 or the backlog.
- **Findings 21–22** (decisions to know about): one `PINNED_UNCLAIMED` WARN
  per phase node, and the strip search counts an unclaimed pin as unplaced.
  Phase 7 touches neither.
- **Finding 16**: the late-finish row reads the day band's block end, not
  `de_total_end`. T036's Schedule view reads the derived schedule, not the
  findings list.

## Rules that bit this feature (still true)

- `tasks.md`: flip checkboxes and add **new** lines starting `  →` only. Never
  reword an existing line — the hook halts on the whole uncommitted diff, so
  commit bookkeeping before any other edit.
- Two subagents must not commit into the worktree at once. Reviewers
  (read-only) can run beside a coding subagent; anything that commits is
  serialised. Phase 6 ran: four parallel red-test dispatches (none
  committing), one Opus implementation that committed them all, a read-only
  test review beside T035's committing reader, one bundled follow-up.
- **Pin the module contract before parallel dispatches.** Phases 3–6 each
  wrote one scratch file naming every field, prop, `data-*` attribute and
  string first. Phase 6's contract was mostly right, and where it was wrong
  (a per-event warning count, a fixture with no possible answer) the
  implementer halted rather than editing an assertion — keep that rule in
  every implementation dispatch.
- Name every test file a dispatch may edit, and what it may change there.
- A red test predicted red that comes back green is a prediction miss to
  record, not a halt; a red test red for the wrong reason is a halt.
- T036 is the only task in phase 7 and it touches `.tsx` and `scripts/smoke.mjs`
  (if the driver reads the Day cell): dispatch `react-code-reviewer` and
  `test-quality-reviewer` after it, and the driver edit is in place, never a
  rewrite (standing rule 5).
- Subagents run commands as `(cd <worktree> && …)`; a fresh worktree has no
  `node_modules` — `pnpm install` first. The editor's diagnostics panel mixes
  the main checkout against worktree types; ignore it, `tsc -b` in the
  worktree is the check.
- Standing rule 13: every surface is styled to the mockup as built, from the
  tokens in `src/index.css`.

## Phase 7 shape

T036 is one red-then-green task: `__tests__/components/scheduleOutput.test.tsx`
gains the day sections, start order, no Day column, every placed event once,
the out-of-range flag, `button` "Print" calling `window.print`, the
page-break class per section and the print-hidden class on the six other
regions. Then `ScheduleOutput.tsx` is restyled after USA Fencing's published
schedules, the Print button added, and `@media print` rules go in
`src/index.css` **or** Tailwind `print:` variants, not both. If the driver
reads the Day cell inside `[data-schedule-row]` at `scripts/smoke.mjs:486`,
re-point it at the section heading. The human print check (quickstart §8) is
T041's, recorded in T043.

Halts: a control deleted without its replacement in the same commit, a driver
locator repaired by rewriting rather than re-pointing, a red test failing for
a reason other than the one predicted, an accessible name or `data-*`
attribute changed by a styling edit.
