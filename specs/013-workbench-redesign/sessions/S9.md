# 013 session 9 – phase 6 (T033 → T035)

Paste into a fresh session started from the **main checkout**
`/Users/noahlz/projects/piste-planner`, not from a worktree.

---

Continue feature 013 (workbench redesign) at phase 6, T033–T035.

**Check the state before trusting this file.** It was written while phase 5's
branch `013-phase5-findings` was still unmerged, at its bookkeeping commit in
the worktree `/Users/noahlz/projects/piste-planner-013-phase5-findings`. If
that branch is now merged into `main` and its worktree removed, everything
below is on `main` and the paths here are stale — `git log --oneline -5`
settles it.

Cut a fresh worktree off `main` for this session (never reuse a previous
session's):

```
git worktree add /Users/noahlz/projects/piste-planner-013-phase6-pins \
    -b 013-phase6-pins main
```

Worktree flow, per the constitution: subagents commit to that branch, the user
makes the merge commit. Then read `specs/013-workbench-redesign/tasks.md` and
`handoff.md` and execute with `/speckit-implement` from **T033**. Stop at the
phase 6 checkpoint after T035.

## State

Phase 5 closed 2026-09-14. `selectFindings` in `src/store/derived.ts` is the
one findings list (Blocking, Unplaced, Warning, Note; dismissed rows filtered),
`FindingsPanel` renders it behind the rail's Findings button with Show on grid
and Dismiss finding, the rail button carries `data-badge`, `Canvas` takes a
committed `findingRows` prop for the gutter flags and the day bands and
scrolls-and-flashes on `jumpNonce`, `Header`'s Auto-assign disables on a
Blocking row, and `AnalysisOutput` is gone. `competitionLabels.ts` and the
block-label helpers now live under `src/lib/`.

Gate at `10568db330` plus the bookkeeping commit: `tsc -b` and lint clean, 74
files / 1722 tests, quickstart §2's `AnalysisOutput` grep silent, drift ledger
and parity unchanged. Run twice — follow-up implementer and orchestrator.

Commits, all on `013-phase5-findings`:

- `3b3e53504c` T030–T032 – eight test files, `selectFindings` and the store
  fields, `FindingsPanel.tsx`, the badge, the jump, the label-helper move,
  `AnalysisOutput` deleted.
- `10568db330` review follow-up – visible flash, StrictMode-safe jump guard,
  the Note map and the tie-break pinned, a real Blocking witness for FR-025.
- the bookkeeping commit – T030–T032 ticked, handoff findings 15–20, four
  comment rewordings that silenced the grep, this file.

## Findings carried into phase 6 (all in handoff.md)

- **Finding 6** (owner decision, still open): no control returns
  `de_mode_override` to null. Phase 5 touched no Settings surface.
- **Finding 13** (owner decision): a placed block is selectable by mouse only.
  Phase 5's Show on grid gives a keyboard user a route to a placed event
  through the panel (finding 20), but the block itself is still `role="img"`
  and not operable. Phase 6 adds Pin behaviour on the detail strip, not on the
  block, so it does not touch this surface.
- **Finding 16** (decision to know about): the late-finish row reads the day
  band's block-end finish, not `de_total_end`. Phase 6's Unplaced-on-collision
  test (T033's `findings.test.ts` addition) reads the same list.
- **Findings 9, 10, 11** unchanged. Finding 11's remainder is phase 6's:
  `Canvas.tsx` reads `placements` live for the pin badge.

## Rules that bit this feature (still true)

- `tasks.md`: flip checkboxes and add **new** lines starting `  →` only. Never
  reword an existing line — the hook halts on the whole uncommitted diff, so
  commit bookkeeping before any other edit.
- Two subagents must not commit into the worktree at once. Reviewers
  (read-only) can run beside a coding subagent; anything that commits is
  serialised. Phases 4 and 5 both ran: parallel red-test dispatches on disjoint
  files (none committing), one implementation dispatch that commits them all,
  two reviewers in parallel, one bundled follow-up that commits.
- **Pin the module contract before parallel dispatches.** Phases 3, 4 and 5
  each wrote one scratch file naming every field, prop, `data-*` attribute and
  string first, and each time the tests and the implementation agreed on first
  contact. Where the contract is silent the tests decide and the implementer
  follows them. Phase 6's contract is largely written already:
  `contracts/engine-contract.md` and data-model §11.
- Name every test file a dispatch may edit, and what it may change there.
  Phase 5's T031 needed import-path edits in seven test files for the label
  move and was told so; T032 was told which three files mount `Canvas`.
- When an implementation task is forbidden from touching a surface the next
  task owns, expect a red test in between and say so in the dispatch. T031's
  stopgap live read in `Canvas` broke FR-042's band test until T032's
  committed prop; both landed in one commit, so the regression never existed
  in history, but the implementer spent effort deciding whether to fix it.
- The engine work in T034 is the one place this feature can move a B1–B8
  count. **Run the ledger before the edit, after it, and read the diff before
  `vitest -u` is even considered.** T035 is a second reader on Opus.
- Subagents run commands as `(cd <worktree> && …)`; a fresh worktree has no
  `node_modules` — `pnpm install` first. The editor's diagnostics panel mixes
  the main checkout against worktree types; ignore it, `tsc -b` in the
  worktree is the check.
- Standing rule 13: every panel and the canvas are styled to the mockup as
  built, from the tokens in `src/index.css`.

## Phase 6 shape

T033 is red tests: `__tests__/engine/pinnedScheduling.test.ts` (seven cases on
B1 with `start_time` computed on the scheduler axis by the caller),
`__tests__/store/runActions.test.ts` (new), a `findings.test.ts` addition
(two colliding pins → the Unplaced row names the second), and a
`stripSearch.test.ts` addition. Case (6), the no-pins result byte-identical
with and without an empty third argument, passes already and must stay green.
Split the red tests into parallel Sonnet dispatches on disjoint files, none
committing. **T034 is Opus and (drift)**: the third `pinned` parameter on both
`scheduleAll` entry points, `PINNED_UNCLAIMED`, the seeded day colouring, the
pre-claim pass, the loop seed skipping pins, `stripSearch` threading, and
`runActions.ts` building the list — every new branch guarded on
`pinned.length > 0`, commit message carrying the eight counts before and
after. **T035 is Opus and (drift)**, a second reader who did not write T034:
ledger and parity against `drift-baseline.md`, the snapshot SHA-256, the
after-table appended.

Halts: **any B1–B8 scheduled count that moves on the no-pins path**, a
snapshot line the commit message cannot explain with its source line, an
import of React or the store under `src/engine/`, a red test failing for a
reason other than the one predicted, a loop without a bound stated before
entry.
