# 013 session 11 – phase 8 (close-out), from T044

Paste into a fresh session started from the **main checkout**
`/Users/noahlz/projects/piste-planner`, not from a worktree.

---

Continue feature 013 (workbench redesign) at phase 8, T044.

**Check the state before trusting this file.** It was written at the phase 7
bookkeeping commit on branch `013-phase7-schedule` in the worktree
`/Users/noahlz/projects/piste-planner-013-phase7-schedule`. If that branch is
now merged into `main` and its worktree removed, everything below is on
`main` and the paths here are stale – `git log --oneline -5` settles it. If
it is **not** merged, stop: the user merges it first (`git merge --no-ff
--no-commit 013-phase7-schedule`, the post-merge check on the merged tree –
`tsc -b`, lint, full suite – then `commit-with-costs`).

Cut a fresh worktree off `main` for this session (never reuse a previous
session's):

```
git worktree add /Users/noahlz/projects/piste-planner-013-phase8-closeout \
    -b 013-phase8-closeout main
```

Worktree flow, per the constitution: subagents commit to that branch, the user
makes the merge commit. Then read `specs/013-workbench-redesign/tasks.md` and
`handoff.md` and execute with `/speckit-implement` from **T044**. T044 is
*(user judges)*: it ends with screenshots the product owner reviews, and the
session stops there for the verdict before T037 onward.

## State

Phase 7 closed 2026-09-14. The Schedule view is one `section` "Schedule"
with a Print button and one `section` "Day N" per day (`data-day-section`,
`print-page`), seven columns with the Day column gone, `@media print` in
`src/index.css` hiding the six chrome regions (`print-hidden`) and un-clipping
the center (`print-unclip`). Every story is built; phase 8 is polish, docs,
the retired-surface grep, the final verification, the human print check and
the handoff.

Checkpoint at `3d35a84e65`: `tsc -b` and lint clean, 76 files / 1747 tests,
SMOKE PASS ×2 with 0 console errors, Suggest 15/66/80/48. Run twice –
implementer and orchestrator.

Commits, all on `013-phase7-schedule`:

- `4f6204d41a` T036 – the view, the print block, the classes, the driver's
  `:525` day read, and three test re-points after a contract miss (finding 26).
- `51df2e7795` smoke – `schedTable` re-pointed at `region` "Schedule" (finding 27).
- `3d35a84e65` review follow-up – the shadcn table container un-clipped in print (finding 28).
- the bookkeeping commit – T036 ticked, handoff findings 26–28, this file.

## Findings carried into phase 8 (all in handoff.md)

- **Findings 6 and 13** (owner decisions, still open): no control returns
  `de_mode_override` to null; a placed block is selectable by mouse only.
  T042 or T043 should record the owner's answer, or that none was given.
- **Finding 25**: `Canvas.tsx` reads `placements` live for the pin badge.
  T044 touches `Canvas` for styling only – if the fix is wanted it is its
  own task, not folded into polish.
- **Finding 26**: before any task deletes a column or a cell, grep
  `data-cell` and `getAllByRole('cell')` under `__tests__/` and name every
  hit in the contract.
- **T041** (print, by hand) is the product owner's. T036's unit test proves
  the classes; nobody has yet printed B1 to PDF.

## Rules that bit this feature (still true)

- `tasks.md`: flip checkboxes and add **new** lines starting `  →` only. Never
  reword an existing line – the hook halts on the whole uncommitted diff, so
  commit bookkeeping before any other edit.
- Two subagents must not commit into the worktree at once. Reviewers
  (read-only) run beside a committing subagent; anything that commits is
  serialised. Phase 7 ran: one red dispatch (no commit), one implementation
  that halted on the contract miss and resumed on authorization, then two
  read-only reviews beside one committing smoke run, then a one-line
  orchestrator follow-up.
- **Pin the module contract before dispatches**, and grep for every consumer
  of anything the contract deletes.
- Name every test file a dispatch may edit, and what it may change there. A
  halt on an unlisted file is the correct outcome, not a failure – the
  orchestrator authorizes the re-point or revises the contract.
- A red test predicted red that comes back green is a prediction miss to
  record, not a halt; a red test red for the wrong reason is a halt.
- Any task touching `.tsx` dispatches `react-code-reviewer`; any task adding
  or editing tests dispatches `test-quality-reviewer`. Driver edits are in
  place, never a rewrite (standing rule 5). Live smoke runs in a subagent.
- Subagents run commands as `(cd <worktree> && …)`; a fresh worktree has no
  `node_modules` – `pnpm install` first. The editor's diagnostics panel mixes
  the main checkout against worktree types; ignore it, `tsc -b` in the
  worktree is the check.
- Standing rule 13: every surface is styled to the mockup as built, from the
  tokens in `src/index.css`. T044 is the pass that finishes this.

## Phase 8 shape

`T044 (user judges) → T037 ‖ T038 → T039 → T040 → T041 ‖ T042 → T043`

T044 is Sonnet, styling only, held to `scripts/smoke.mjs` passing with no
locator edit and no accessible name, role, `data-*` or DOM-order change. It
ends with `scripts/screenshot.mjs` shots sent to the product owner and a
stop. T038 is the orchestrator's read-only grep. T039 is a dispatched smoke
run. T040 is the twice-run full check with the test-count reconciliation
against T001's 68 files / 1848 tests. T041 is the owner's print check. T042
and T043 are docs; T043 marks `spec.md` Delivered and writes the merge
instructions.

Halts: a polish edit that changes an accessible name, role, `data-*` or DOM
order; a driver locator repaired by rewriting; a test count that does not
reconcile; a `.skip`/`.only` anywhere under `__tests__/` or `src/`.
