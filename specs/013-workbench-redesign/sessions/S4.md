# 013 session 4 – record the T015b re-look, then phase 2 (T016 → T023)

Paste into a fresh session started from the **main checkout**
`/Users/noahlz/projects/piste-planner`, not from the worktree.

---

Read `specs/013-workbench-redesign/tasks.md` and `handoff.md` in the worktree
and continue executing with `/speckit-implement` from **T016**, worktree
flow, one Sonnet subagent per task (Opus where tasks.md marks it). Phase 2 is
T016–T023; stop at the phase 2 checkpoint after T023.

State: the worktree `/Users/noahlz/projects/piste-planner-013-workbench-redesign`
(branch `013-workbench-redesign`) is at `785327a42f`. Commits since S3:
- `51dfaa5392` – T015 checkpoint: screenshot script, verdict, the polish re-plan.
- `59dbfa71fc` – T015a: chrome polished to the mockup. shadcn core variables
  re-pointed to the mockup's palette; Industry ramps `--accent-100…900` and
  `--neutral-100…900` mapped in `@theme inline` (deliberately replacing
  Tailwind's default neutral ramp – nothing used it); chrome roles
  `--chrome`, `--chrome-border`, `--chrome-deep`, `--hover-tint`, `--ok`; the
  twelve `--weapon-{foil,epee,sabre}-{fill,ink,edge,hatch}` tokens added now
  with the mockup's values so **T024 finds them present and only writes
  `weaponTokens.ts`**. Export, the five rail buttons and the panel's
  Dock/Float/Close are icon-only with `aria-label`+`title` carrying the
  byte-identical names. 75 files / 1842 tests, tsc and lint clean, SMOKE PASS,
  0 console errors, no driver edit, Suggest 15/63/80/48.
- `785327a42f` – T015a review follow-up: shadow tokens live only in Tailwind's
  `--shadow-*` theme namespace (the `:root` copies self-referenced); footer
  toggle uses `bg-secondary`.

T015b ran at `785327a42f`: SMOKE PASS, 0 console errors, no driver edit,
shots at `scripts/smoke-shots/shell-{1440x900,1920x1080}.png` sent to the
product owner. **The re-look verdict is: matches.** Uncommitted: `tasks.md` carries one new `→` line under
T015b saying the shots were sent and the verdict was pending. `sessions/` is
never committed.

First step (orchestrator, small edits, then a Sonnet subagent commits):
1. In `handoff.md` §Verdicts, under the T015 verdict, add a "T015b re-look
   (2026-09-07)" paragraph with the verdict above; anything listed as still
   off is carried into T044's scope (tasks.md phase 8) – add it there as a
   new `  →` line under T044, never by rewording T044.
2. Tick T015b in `tasks.md` and add one `  →` line with the measured result
   (SMOKE PASS, 0 console errors, no driver edit, the verdict in one clause).
3. Commit `handoff.md` and `tasks.md` as
   `013 T015b: the polish changed nothing but paint; product owner's re-look recorded`,
   trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.
   This is the phase 1 checkpoint. Phase 2 starts regardless of the verdict
   (no second chrome round before T016 – S3 decision).

Then phase 2, per tasks.md. Things it must not lose:
- Standing rule 13: every panel T016–T022 builds is styled to the mockup as it
  is built, from the tokens now in `src/index.css` (`docs/design/mockup/`
  lines 128–270 hold the panel bodies: Tournament, Strips, Events, Findings,
  Settings). Structure follows alignment doc §9 where they disagree.
- T023 records NAC Youth's Suggest count – expected 66 once T022 deletes the
  driver's Admin gap step; a different value is recorded, not corrected.
- Finding 1 (lane packer vs scheduler: B1 boots 24 rows vs footer
  `19 placed · 5 unplaced`) is US3's problem; do not chase it in phase 2.
- The smoke driver's pages share `localStorage`; `openPanel` guards on
  `aria-pressed` for every page.

Rules that bit this feature already:
- `tasks.md`: flip checkboxes and add **new** lines starting `  →` only.
  Never append to or reword an existing line – the hook halts on the whole
  uncommitted diff, so commit the checkpoint before any other edit.
- Subagents run commands as `(cd <worktree> && …)`; a smoke run starts the
  worktree's own dev server on a spare port (`pnpm dev --port 5174`) and sets
  `SMOKE_BASE` to it, because port 5173 may be the main checkout.
- Reviews after every task that adds tests or touches `.tsx`; bundle review
  fixes into one dispatch that commits as a `follow-up`.
- The editor's diagnostics panel reports the main checkout's deleted-in-
  worktree tests (`Scorecard.test.tsx`) against worktree types; ignore them,
  `tsc -b` in the worktree is the check.
