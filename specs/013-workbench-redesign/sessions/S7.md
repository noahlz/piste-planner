# 013 session 7 – phase 4 (T028 → T029)

Paste into a fresh session started from the **main checkout**
`/Users/noahlz/projects/piste-planner`, not from the worktree.

---

Read `specs/013-workbench-redesign/tasks.md` and `handoff.md` in the worktree
`/Users/noahlz/projects/piste-planner-013-workbench-redesign` and continue
executing with `/speckit-implement` from **T028**, worktree flow, one Sonnet
subagent per task (nothing in phase 4 is marked Opus). Phase 4 is T028–T029;
stop at the phase 4 checkpoint after T029.

State: the worktree (branch `013-workbench-redesign`) is at `e20ec376da`,
clean except the untracked `sessions/` directory (never committed). Phase 3
closed 2026-09-13: the SVG canvas and its windowing, zoom, palette and labels
are gone, the HTML canvas draws every strip row with sticky axis, bands and
gutter, the six-rung ladder and Fit day live in the footer, blocks carry
weapon fill with phase hatch and icons, and the driver passed four times
(two at `b9f9ad46b8`, two at `2eda25bc66`). Gate at HEAD: `tsc -b` and lint
clean, 71 files / 1671 tests.

Commits since S6 (all on the worktree branch):
- `05103d5ff4` T024–T026 – `Canvas.tsx`, `Block.tsx`, `zoomLadder.ts`,
  `weaponTokens.ts`, `selectDaySummaries`, view state on `zoomStep` +
  `fitting`, the footer's `toolbar` "Zoom", driver "Fit to day" → "Fit day".
- `01765a3087` review follow-up – day bands draw from the committed lanes
  (`daySummariesFromBlocks`), shared `__tests__/helpers/resizeObserver.ts`.
- `41dfe0e31f` T027 live-run defect – a block with no room for text keeps
  its phase icon, shrinking to a 10 px floor.
- `b9f9ad46b8` T027 – driver PASS ×2 with the SC-005 zoom read; ten prose
  mentions of retired names reworded.
- `ce540ffd56` follow-up 2 – block padding and gap applied inline as the
  mockup does, empty label span not rendered, floor pinned at 10/9 px.
- `2eda25bc66` driver reads an absent label as empty; PASS ×2.
- `3b24c851e8`, `e20ec376da` bookkeeping.

## Findings carried into phase 4 (all in handoff.md)

- **Finding 6** (owner decision, still open): no control returns
  `de_mode_override` to null. Third "Default" pill, a revert, or accept.
- **Finding 9**: finding 1 is closed – the packer and the counter agree. B1 at
  80 strips: 19 placed + 5 packer-overflow = 24; the five draw at strip 0
  dashed. Whether an overflowed block also belongs in the dock is phase 6's.
- **Finding 10**: `D1A-M-SABRE-IND:DE_PRELIMS` is a 5-minute segment over 12
  strips. The canvas now handles it (icon alone). The segment is the known
  engine defect in `docs/design/backlog.md` §DE prelims gets a sliver of its
  bracket's time (`deBlockDurations` bout accounting); behind the drift
  ledger, its own spec, not this feature's.
- **Finding 11**, the recorded remainder: `Canvas.tsx` reads `placements`
  live for the pin badge. Phase 6 owns pinning and decides.
- **Phase 4 touches the canvas**: T028 adds a click-selects case to
  `Canvas.test.tsx` and T029 wires the click in `Canvas.tsx` and mounts the
  detail strip under it in `CenterView.tsx`. `data-selected` already exists
  on `Block` (T026) fed by a `selected` prop; the store field arrives in T029.

## Rules that bit this feature (still true)

- `tasks.md`: flip checkboxes and add **new** lines starting `  →` only.
  Never reword an existing line – the hook halts on the whole uncommitted
  diff, so commit bookkeeping before any other edit. Orchestrator
  bookkeeping commits in the worktree are fine (`3b24c851e8`, `e20ec376da`).
- Two subagents must not commit into the worktree at once. Run reviewers
  (read-only) beside a coding subagent, but serialise anything that commits.
  A subagent that edits while another holds uncommitted work must `git add`
  its own paths only. Phase 3's pattern: red-test dispatches in parallel
  (none commits), the implementation dispatch commits them all.
- Subagents run commands as `(cd <worktree> && …)`; the smoke run starts the
  worktree's own dev server on 5174 (`pnpm dev --port 5174 --strictPort`)
  with `SMOKE_BASE=http://localhost:5174/piste-planner/`.
- Reviews after every task that adds tests or touches `.tsx`; bundle the
  fixes into one dispatch that commits as a `follow-up`. A live-run defect
  is fixed in the app, reviewed, and the driver rerun on the final tree.
  Phase 3 needed two rounds of that (finding 10); budget for it.
- Pin the module contract before parallel red-test dispatches. Phase 3 wrote
  one scratch file naming every prop, attribute and export, and the tests
  and the implementation agreed on first contact. Where the contract guesses
  (the tick ladder, the findings id scheme) the implementer measures and the
  commit records the correction.
- The driver's pages share `localStorage`; `openPanel` guards on
  `aria-pressed`. Any driver step that leaves a tournament-wide override
  behind must restore it. The app boots in fit mode, so a zoom step that
  presses "Fit day" first changes nothing – "Zoom in" first.
- Playwright locator reads auto-wait: a `[data-*]` element that is
  legitimately absent must be `count()`-guarded (`2eda25bc66`).
- The editor's diagnostics panel reports the main checkout's
  deleted-in-worktree files against worktree types; ignore them, `tsc -b`
  in the worktree is the check.
- Standing rule 13: every panel and the canvas are styled to the mockup as
  built, from the tokens in `src/index.css`; the mockup is
  `docs/design/mockup/Piste Planner Workbench.dc.html`. A polish never
  changes an accessible name, role, `data-*` attribute or DOM order.

## Phase 4 shape

T028 (red tests across `selection.test.ts`, `DetailStrip.test.tsx`,
`viewState.test.ts`, `Canvas.test.tsx`, `UnplacedDock.test.tsx`,
`derived.test.ts`, `buildConfig.test.ts`, `serialization.test.ts`) is large:
split it into two or three parallel Sonnet dispatches on disjoint files, none
committing, with a contract file pinning `selectedCompetitionId`,
`selectCompetition`, `setPinned`, `detailCollapsed`, the `DetailStrip` props
and every `data-selected-*` attribute from ui-contract §Detail strip. T029
implements and commits them all, then deletes the `AnalysisSlice` and the
`flightingSuggestions` parameter in the same commit (standing rule 4).
Checkpoint: `tsc -b`, lint, full suite, and the quickstart §2 grep for
`flightingSuggestion` returns nothing. Phase 4 does not ship without phase 6
(spec US4); that is a merge question, not a reason to hold T028.
Halts: a polish that changes an accessible name or `data-*` attribute, a
retired name surviving the phase grep, a driver locator repaired by
rewriting, a red test failing for a reason other than the one predicted.
