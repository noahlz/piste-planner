# 013 session 3 – commit the T015 checkpoint, then T015a → T015b → phase 2

Paste into a fresh session started from the **main checkout**
`/Users/noahlz/projects/piste-planner`, not from the worktree.

---

Read `specs/013-workbench-redesign/tasks.md` and `handoff.md` in the worktree
and continue executing with `/speckit-implement` from **T015a**, worktree
flow, one Sonnet subagent per task. **Stop after T015b** for my screenshot
re-look.

State: the worktree `/Users/noahlz/projects/piste-planner-013-workbench-redesign`
(branch `013-workbench-redesign`) is at `37987dc5dc` with T001–T014 committed
(T014 at `30c7c452b0` plus two review follow-ups on the canvas tooltip fix).
75 files / 1842 tests, tsc and lint clean, smoke driver passes twice with 0
console errors. My T015 verdict is **yes, with a polish pass**, recorded in
`handoff.md` §Verdicts with its decisions (chrome now and everything at
close-out; styling only; the mockup copied into the repo). The previous
session re-planned on that verdict and stopped: `tasks.md` gained T015a and
T015b in phase 1, T044 in phase 8, standing rule 13, and matching rows in
Dependencies, Model split and What halts a task.

Uncommitted in the worktree, all verified, all belonging to T015's checkpoint:
`scripts/screenshot.mjs` (new, the repo artifact that took the shots),
`specs/013-workbench-redesign/handoff.md` (new: verdicts, findings,
measurements), `specs/013-workbench-redesign/tasks.md` (T015 ticked and the
re-plan), and `docs/design/mockup/` (new: `Piste Planner Workbench.dc.html`,
`ds-styles.css`, `ds-readme.md`, `ds-manifest.json`, `README.md`, fetched from
my Claude Design project on 2026-09-07 – confirm the directory exists and has
those five files before anything else; if it does not, fetch them with the
`DesignSync` tool, `get_file`, project `f4e0ca5c-b7ea-4d4a-a215-6341ad1179d4`).
`sessions/` is never committed.

First step (Sonnet subagent, commits): stage exactly those four paths and
commit as `013 T015: screenshot script, product owner's verdict, and the
polish re-plan (T015a, T015b, T044, rule 13)`, body naming the verdict and the
decisions, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`.

Decisions for T015a and T015b, taken 2026-09-07 and recorded in
`handoff.md` §Verdicts:
- **Type: a system-font stack, no font files.** The mockup uses Figtree and
  its design system uses Barlow, both from Google Fonts, and plan.md allows no
  new dependency. Take the mockup's sizes, weights, letter-spacing and line
  heights and set them on a `system-ui` stack; do not vendor or link a web
  font. Letterforms will differ from the mockup and that is accepted.
- **Stop after T015b** for my re-look of the polished chrome.
- **If the re-look finds the chrome still off, list it for T044 and keep
  going.** Phase 2 starts regardless; no second chrome round before T016.

Then T015a (Sonnet, commits): port the mockup's tokens into `src/index.css`
and restyle the header, rail, dock, footer, preset picker, export trigger and
panel chrome to `docs/design/mockup/` – styling only, no accessible name,
role, `data-*` or DOM-order change, the driver must pass without a locator
edit, the Export button must stop being white-on-white. Dispatch
`react-code-reviewer` after it and bundle fixes into one follow-up commit.

Then T015b (Sonnet, dispatched): run `scripts/smoke.mjs` once and
`scripts/screenshot.mjs`, send me both shots, record my re-look under the
T015 verdict in `handoff.md`. Not a halt – differences go to T044's scope.

Findings already in `handoff.md` that later phases must not lose:
- B1 at boot: schedule table 24 rows, footer `19 placed · 5 unplaced`; the
  dock says every event has a slot. Lane packer vs scheduler – US3's problem.
- NAC Youth Suggest reads 63 while the Admin gap driver step exists; D14
  expects 66 once phase 2's Settings task deletes that step – T023 records it.
- The smoke driver's pages share `localStorage`, so `openPanel` guards on
  `aria-pressed` for every page.

Rules that bit this feature already:
- `tasks.md`: flip checkboxes and add **new** lines starting `  →` only. Never
  append to or reword an existing line – the hook halts on the whole
  uncommitted diff, so commit the checkpoint before any other edit.
- Subagents run commands as `(cd <worktree> && …)`.
- Reviews after every task that adds tests or touches `.tsx`; bundle review
  fixes into one dispatch that commits as a `follow-up`.
- Standing rule 13: every surface built from here is styled to the mockup as
  it is built; alignment doc §9 wins on structure.
