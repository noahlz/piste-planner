# 013 session 6 – phase 3 (T024 → T027)

Paste into a fresh session started from the **main checkout**
`/Users/noahlz/projects/piste-planner`, not from the worktree.

---

Read `specs/013-workbench-redesign/tasks.md` and `handoff.md` in the worktree
`/Users/noahlz/projects/piste-planner-013-workbench-redesign` and continue
executing with `/speckit-implement` from **T024**, worktree flow, one Sonnet
subagent per task (Opus where tasks.md marks it – T026 is marked). Phase 3 is
T024–T027; stop at the phase 3 checkpoint after T027.

State: the worktree (branch `013-workbench-redesign`) is at `f33b851ac7`,
clean except the untracked `sessions/` directory (never committed). Phase 2
closed 2026-09-13: every input has one home, the payload is v3, the five
section components are gone, the phase grep returns nothing, and the driver
passed three times (two T023 runs at `9da51b1b15`, one confirming run at
`e4fcd29058`). Gate at HEAD: `tsc -b` and lint clean, 72 files / 1840 tests.

Commits since S5 (all on the worktree branch):
- `a85e14f5f5` T020 – `CompetitionConfig = { fencer_count, flighted }`,
  `buildConfig.ts` derives the six retired fields, payload v3, v2 refused.
  `appPathParity.test.ts` passed unedited.
- `8f670eba53` T016–T018 review follow-up (five findings).
- `c956bde6bb` T021 – `EventsPanel` replaces `CompetitionMatrix` and
  `FencerCounts`; `configEditing.test.tsx` deleted with successors checked.
- `260be6b22f` T022 – `panels/SettingsPanel`, `de_mode_override` on the
  tournament slice, `globalOverrides` gone everywhere, `deModeLabels.ts`
  deleted, driver Admin gap steps replaced.
- `df977bf487` T020–T022 review follow-up – `EventChip` subscribes per entry
  (render-count test), absent `de_mode_override` round-trips, comment fixes.
- `9da51b1b15`, `e4fcd29058` – Strips-panel follow-ups found by T023's live
  run: the suggested minimum clears the moment an input changes, and an
  input change invalidates the search in flight (token bump + indicator
  reset).
- `d8035a8c61` T023 – smoke passes twice, NAC Youth records **66**, SC-008
  holds at 80; driver restores DE mode to Staged after the round-trip.
- `f934b64c79` confirming run; `f33b851ac7` bookkeeping.

## Findings carried into phase 3 (all in handoff.md §Findings carried)

- **Finding 1 is US3's**: B1 boots 24 schedule rows vs footer
  `19 placed · 5 unplaced` – the lane packer and the scheduler disagree.
  Phase 3 rewrites the canvas and its packing; resolve or re-measure there.
- **Finding 6**: no control returns `de_mode_override` to null. Once Staged
  or Single is picked the tournament stops following its type. Spec FR-030
  and ui-contract §Settings name only two pills. Product-owner decision:
  third "Default" pill, a revert, or accept. Not phase 3's; raise it.
- **Finding 7**: the team-cut coercion loop in `buildConfig.ts` is an
  unreachable backstop; no test proves it fires. Backlog.
- **Finding 8**: the DE-mode leak and the stale-suggestion race, both fixed;
  the driver now restores Staged after the round-trip.

## Rules that bit this feature (still true)

- `tasks.md`: flip checkboxes and add **new** lines starting `  →` only.
  Never reword an existing line – the hook halts on the whole uncommitted
  diff, so commit bookkeeping before any other edit. Orchestrator
  bookkeeping commits in the worktree are fine (`f33b851ac7`).
- Two subagents must not commit into the worktree at once. Run reviewers
  (read-only) beside a coding subagent, but serialise anything that commits.
  A subagent that edits while another holds uncommitted work must `git add`
  its own paths only.
- Subagents run commands as `(cd <worktree> && …)`; the smoke run starts the
  worktree's own dev server on 5174 (`pnpm dev --port 5174 --strictPort`)
  with `SMOKE_BASE=http://localhost:5174/piste-planner/`.
- Reviews after every task that adds tests or touches `.tsx`; bundle the
  fixes into one dispatch that commits as a `follow-up`. A live-run defect
  is fixed in the app, reviewed, and the driver rerun on the final tree.
- The driver's pages share `localStorage`; `openPanel` guards on
  `aria-pressed`. Any driver step that leaves a tournament-wide override
  behind must restore it (T023's Staged restore is the pattern).
- The editor's diagnostics panel reports the main checkout's
  deleted-in-worktree files against worktree types; ignore them, `tsc -b`
  in the worktree is the check.
- tasks.md's driver line numbers are stale (the file is ~1080 lines);
  locate steps by content.
- Standing rule 13: every panel and the canvas are styled to the mockup as
  built, from the tokens in `src/index.css`; the mockup is
  `docs/design/mockup/Piste Planner Workbench.dc.html`.

## Phase 3 shape

T024 `[P]` (ladder + weapon tokens, red first) and T025 (red tests for the
canvas) can run together – neither commits; T026 (Opus) builds the canvas
and commits them all. Then T027 (the driver, dispatched) per tasks.md.
Halts: any B1–B8 count
moving on the no-pins path, a polish that changes an accessible name or
`data-*` attribute, a retired name surviving the phase grep, a driver
locator repaired by rewriting.
