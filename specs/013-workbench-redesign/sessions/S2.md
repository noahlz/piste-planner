# 013 session 2 – resume at T014

Paste into a fresh session started from the **main checkout**
`/Users/noahlz/projects/piste-planner`, not from the worktree.

---

Read `specs/013-workbench-redesign/tasks.md` and continue executing it with
`/speckit-implement` from **T014**, worktree flow, one Sonnet subagent per
task. Stop after T015 for my screenshot verdict.

State: the worktree `/Users/noahlz/projects/piste-planner-013-workbench-redesign`
(branch `013-workbench-redesign`) is at `a3367af740` with T001–T013 committed,
75 test files / 1838 tests, tsc and lint clean. Four files are uncommitted and
already verified: the T010–T011 review fixes in `src/store/derived.ts`
(`selectPlacementCounts` counts an overflowing event once and excludes it from
`placed`, so placed + unplaced equals the selected count),
`__tests__/store/footerMetrics.test.ts`, `__tests__/components/workbench/Header.test.tsx`,
and one `→` annotation line in `tasks.md`. T014's checkpoint commit carries
them. `scripts/smoke.mjs` is untouched and nothing serves port 5173.

Rules that bit this feature already:
- `tasks.md`: flip checkboxes and add **new** lines starting `  →` only. Never
  append to or reword an existing line – the hook reads the whole uncommitted
  diff and halts every later edit until it is committed.
- Subagents run commands as `(cd <worktree> && …)`; `pnpm -C` misbehaved.
- Every old section component is still mounted behind a rail button
  (Tournament → `TournamentSetup`, whose type control is labelled "Type";
  Strips & referees → `StripSetup` + a `region` "Advanced"; Events →
  `CompetitionMatrix`, `FencerCounts`, `CompetitionOverrides`; Findings →
  `AnalysisOutput`; Settings → the old `SettingsPanel` as `region` "Settings"
  inside the `aside` "Inspector panel"). The driver must open the panel before
  reaching any of them, including `pressSuggest`. NAC Youth should still read
  63 in this phase.

T014 (dispatched, subagent commits): re-point `scripts/smoke.mjs` in place per
the task's map – Export popover, footer `data-counts`/`data-metric`, header
`data-summary`, hover step deleted, "Auto-assign", rail buttons for Settings,
Strips, Tournament, Events – start `pnpm dev` from the worktree, run the driver
twice, report SMOKE PASS ×2, the four Suggest counts, boot placed count and 0
console errors, then commit with the review fixes.

T015 (dispatched, user judges): add `scripts/screenshot.mjs` as a repo
artifact reusing `smoke.mjs`'s Chrome discovery – boot the app, wait for B1's
blocks, save full-window screenshots at 1440×900 and 1920×1080 under
`scripts/smoke-shots/` – send both to me and **stop**. Record my verdict in
`specs/013-workbench-redesign/handoff.md` §Verdicts (create the file). A "no"
halts phase 3 and is a re-plan: record it, hand back a resume prompt, stop.

Findings to carry into `handoff.md`: B5 at 60 strips shows 9 placed / 3
unplaced because the lane packer cannot fit three DE blocks the scheduler
placed; B4's app path places 18 against the ledger's 17 (a recorded parity
exception, not drift); T010 was written green-first.
