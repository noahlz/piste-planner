# 013 session 5 – finish phase 2 (T020 → T023)

Paste into a fresh session started from the **main checkout**
`/Users/noahlz/projects/piste-planner`, not from the worktree.

---

Read `specs/013-workbench-redesign/tasks.md` and `handoff.md` in the worktree
and continue executing with `/speckit-implement` from **T020**, worktree
flow, one Sonnet subagent per task (Opus where tasks.md marks it). Phase 2 is
T016–T023; stop at the phase 2 checkpoint after T023.

State: the worktree `/Users/noahlz/projects/piste-planner-013-workbench-redesign`
(branch `013-workbench-redesign`) is at `1e730723b7`. Commits since S4:
- `c96049a60a` – T015b: phase 1 checkpoint. Product owner's re-look verdict
  "matches" recorded in `handoff.md` §Verdicts; nothing carried into T044.
- `1e730723b7` – T016–T018: `TournamentPanel` (Radix RadioGroup; the
  out-of-range day pill reads the engine's `days-available-range` finding),
  `computeSuggestedStrips(): Promise<number | null>` replacing `suggestStrips`
  (never writes; `setStrips` writes), `StripsPanel` (steppers, Suggested
  minimum card: search on mount + 300 ms debounce, stale tokens discarded,
  reveal-delayed `role="status"`, Apply writes; `data-refs-per-pool`).
  `TournamentSetup`, `StripSetup`, `AdvancedPanel` deleted with their tests.
  75 files / 1849 tests, tsc and lint clean. Driver: type step → `radio`
  "ROC"; `pressSuggest` → open panel, wait `data-suggested-strips`, Apply.
  **The driver is not runnable between this commit and T020's** – its
  per-event `Referees for` steps (~`scripts/smoke.mjs:950–1035`) still
  target the deleted AdvancedPanel; T020 deletes them by name.

**Uncommitted in the worktree** (all ride T020's checkpoint commit):
- `tasks.md`: T016, T017, T018 ticked with `→` result lines (bookkeeping only).
- T019's red tests, if its subagent finished (see §T019 below). Check
  `git status --short` first; if T019's files are absent, dispatch T019
  before T020 per tasks.md.
- `sessions/` is never committed.

## T019 (red tests for the shrink) – dispatched at `1e730723b7`

Its report is at the bottom of this file if it arrived before the session
closed. What it was told: `buildConfig.test.ts` gains a derivations describe
with a **captured** pre-shrink fixture under `__tests__/fixtures/` (deep-equal
against live output, never hand-written); `serialization.test.ts` gains the
v3 round trip and the v2 refusal; `EventsPanel.test.tsx` is new (heading
`Selected N of 120`, one `section` per gender+weapon, chip order individual →
six veteran bands → Team, `aria-pressed` chips, `spinbutton` "Fencer count
for {label}" with `min={MIN_FENCERS}` refusing smaller, no per-event
cut/DE/video/referee/single-pool control); `buildConfig.typeDefaults`,
`typeDefaultPrecedence`, `competitionDefaults` tests re-targeted at derived
output. T020 removes any `@ts-expect-error` T019 placed on `flighted`.

## Review findings for T016–T018 – bundle into ONE follow-up dispatch after T020's checkpoint, committed as `013 T016–T018 follow-up: …`

React review (`1e730723b7`), two medium:
1. `StripsPanel.tsx:111–128` `runSearch` has `try/finally` with no `catch`;
   both call sites are `void runSearch()`, so a throw from
   `computeSuggestedStrips` is an unhandled rejection. Fix: catch, set the
   suggestion to `null`, keep the `finally`.
2. `TournamentPanel.tsx:76` subscribes to `selectDerivedFindings` (memo key =
   the whole schedule), so any edit elsewhere recomputes findings and
   re-renders the panel. Fix: a narrow selector for the
   `days-available-range` rule keyed on `tournament_type`/`days_available`
   only (or compute the notice from those two fields directly – the engine
   rule at `validation.ts:421` is a range check).
Test-quality review (`1e730723b7`), one medium, two low:
3. `StripsPanel.test.tsx:167–183` "coalesces two changes inside the debounce
   window" runs both store writes and the timer advance inside one `act`, so
   React batches them and no first timer is ever scheduled then cancelled –
   the case passes with the debounce cleanup deleted. Fix: split into
   separate `act()` calls (write, advance 250 ms, write, advance the rest)
   like the adjacent 299/300 ms case at `:118–135`.
4. `StripsPanel.test.tsx:296–299` asserts the status text with an unanchored
   `toMatch`; make it `toBe('Searching for the smallest strip count that
   places every event…')`.
5. Coverage drop not named in the commit: the video-strips field
   re-resolving at a type whose row is 0 (the two ROC cases from
   `AdvancedPanel.test.tsx`). Add one case: `setTournamentType(ROC)` with
   `video_strips_total` null → the spinbutton reads 0; an explicit count
   survives the type change. Name it in the follow-up commit.
Verified correct by the reviewer: stale-token, 299/300 ms boundary, unmount
timer count, the no-write subscribe case, no colour pinned.

Then T021, T022, T023 per tasks.md. Things it must not lose:
- Standing rule 13: every panel is styled to the mockup as it is built, from
  the tokens in `src/index.css`. `docs/design/mockup/Piste Planner
  Workbench.dc.html` lines 200–223 hold the Events panel body (caption
  "Selected" + monospace "N of 120"; one card per group: `border-[1.5px]
  border-chrome-border rounded-xl bg-white`, header row `bg-[--chrome-deep]`
  uppercase 11.5px with a monospace count, chips wrapped at 5px gap) and
  lines 224–250 the Settings body (Pool durations card with per-row
  monospace values, DE mode pills Staged/Single, a Video strips line).
  Follow `TournamentPanel.tsx`'s caption and pill conventions.
- T023 records NAC Youth's Suggest count – expected 66 once T022 deletes the
  driver's Admin gap step; a different value is recorded, not corrected.
- Finding 1 (lane packer vs scheduler: B1 boots 24 rows vs footer
  `19 placed · 5 unplaced`) is US3's problem; do not chase it in phase 2.
- The smoke driver's pages share `localStorage`; `openPanel` guards on
  `aria-pressed` for every page.

Rules that bit this feature already:
- `tasks.md`: flip checkboxes and add **new** lines starting `  →` only.
  Never append to or reword an existing line – the hook halts on the whole
  uncommitted diff, so commit the checkpoint before any other edit.
- Subagents run commands as `(cd <worktree> && …)`; a smoke run starts the
  worktree's own dev server on a spare port (`pnpm dev --port 5174`) and sets
  `SMOKE_BASE` to it, because port 5173 may be the main checkout.
- Reviews after every task that adds tests or touches `.tsx`; bundle review
  fixes into one dispatch that commits as a `follow-up`.
- The editor's diagnostics panel reports the main checkout's deleted-in-
  worktree files (`Scorecard.test.tsx`, `AdvancedPanel.tsx`, `StripSetup.tsx`)
  against worktree types; ignore them, `tsc -b` in the worktree is the check.
- tasks.md's driver line numbers are stale (the file is ~1040 lines); locate
  steps by content.

## T019 report (arrived before close – its files are uncommitted in the worktree)

- Created `__tests__/fixtures/buildConfig-preShrink-nac-vet-div1-junior.json`
  – captured at `1e730723b7` by a throwaway vitest file (deleted) that applied
  template `NAC Vet/Div1/Junior` (66 competitions: individual, all six
  veteran bands, team), set every `fencer_count` to 40, ran
  `buildTournamentConfig` at NAC and at ROC, sorted by id. `Infinity` is
  written as the string `"__Infinity__"`; the test applies the same
  substitution to the live result before deep-equal.
- Created `__tests__/components/workbench/panels/EventsPanel.test.tsx`
  (11 cases, red on the missing module).
- `buildConfig.test.ts` 24 → 31: frozen-baseline deep-equal at NAC and ROC,
  per-field derivation checks, and `flighted mirrors the store's flag`
  (`:681`, red `expected false to be true`; the `updateCompetition` call
  carries `// @ts-expect-error` **that T020 removes**).
- `serialization.test.ts` 81 → 85: v3 round trip (`:1075`, red `expected 2
  to be 3`) and v2 refusal (`:1091`, red). Existing v2-shaped cases
  untouched – T020 re-targets them; grep `schemaVersion`,
  `selectedCompetitions`, `globalOverrides`, `ref_policy`, `de_mode`,
  `cut_mode`, `de_video_policy` in that file (the reported line list was
  truncated; `:22` type, `:30` local `SerializedStateV2`, `:86` …).
- `buildConfig.typeDefaults.test.ts` 11 → 9 (dropped: explicit `ref_policy`
  / `de_mode` survive – no per-event override remains).
- `typeDefaultPrecedence.test.ts` 8 → 4 (dropped: `ref_policy TWO` and
  `de_mode STAGED` survive a walk, and the "regional cut override beats
  explicit cut" describe, two cases).
- `competitionDefaults.test.ts` 5 → 5: C2/C3 read cut off
  `buildTournamentConfig` output.
- Battery: 3 files red / 3 green, 3 failed / 444 passed – the frozen-baseline
  and derivation cases pass today because the current defaults equal what
  T020 hard-codes. `tsc -b`: one error, the missing `EventsPanel.tsx`.

T020's commit message must name the four dropped-without-successor cases and
the fixture's capture method.
